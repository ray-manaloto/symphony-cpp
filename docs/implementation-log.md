# Implementation goal log

Append-only record for the standalone implementation.

| Date | Observation | Decision |
| --- | --- | --- |
| 2026-07-22 | A new repository avoids coupling the C++ service to the honeymoon Node controller. Both research lanes already have current artifacts. | Bootstrap a fixture-only C++ vertical slice; mark primary and Last30Days research `reused`; prohibit live mutations and publication. |
| 2026-07-22 | The initial portable Linux C++26 suite passed, then dependency-enabled Docker commands detached after the tool window and overlapping invocations corrupted Ninja's log. | Treat long compiler/build jobs as named containers and poll one owner; never start a duplicate for an unchanged running build. This is `promoted` into `scripts/container-job.sh`. |
| 2026-07-22 | Repository creation already triggered the compiler matrix; a manual dispatch queued the same revision behind it. | Cancel the duplicate immediately. Before dispatch, list runs for the exact workflow and commit; dispatch only when no queued or running equivalent exists. |
| 2026-07-22 | GCC 16.1 surfaced a deprecation warning inside nlohmann/json under the project's `-Werror` policy. | Mark fetched third-party dependencies as CMake `SYSTEM`; retain strict warnings for project source. |
| 2026-07-22 | The owner wants to choose the C++ infrastructure libraries before the scaffold expands. | Freeze the library decision boundary; continue only architecture-neutral protocol, conformance, source-lock, and CI work until those choices are agreed. |
| 2026-07-22 | The workspace-level commit hook evaluates this standalone repository against an expired controller lease in the separate honeymoon worktree. | Do not bypass the hook or mutate the other worktree. Preserve the staged changes and treat repository-local commit authorization as unresolved infrastructure. |
| 2026-07-22 | The owner selected Glaze, `openalgz/ut`, and vcpkg, and requested modern hosted async/persistence candidates plus broad static analysis. | Adopt one Glaze codec seam, keep Codex on JSONL/JSON-RPC, migrate dependencies through a pinned vcpkg manifest, migrate tests to `ut`, and evaluate SQLite plus a serialized executor before a bounded `sqlgen` spike. Reject the Intel bare-metal stack for the hosted daemon. |
| 2026-07-22 | The recent-practitioner scan was noisy and its web/YouTube lanes failed, while primary project documentation directly answered the architecture questions. | Record `no-new-lesson`: retain the raw report as discovery evidence, supplement it with owning sources, and do not let social ranking override primary documentation. |
| 2026-07-22 | GCC 16.1 rejected an allocation-backed reflection query used directly as an expansion-statement range. | Bind `nonstatic_data_members_of` to a `constexpr` local before each expansion, matching current P2996 usage; require the remote GCC 16 reflection job as the regression guard. |
| 2026-07-22 | The first reflection correction still retained the allocation-backed range and failed on GCC 16.1. GCC's implementation/tests require static promotion for expansion. | Correct the prior decision: wrap each reflected member range with `std::define_static_array`; preserve the failed CI run as regression evidence and rerun the exact remote job. |
| 2026-07-22 | The owner selected `klemens-morgenstern/sqlite`; source inspection found strong typed/RAII SQLite coverage but no async API or curated vcpkg port. | Adopt it only inside `symphony_persistence`, package it through a pinned overlay, and execute it on the serialized DB executor behind repository-owned sender/error/schema boundaries. |
| 2026-07-22 | Custom bootstrap mechanisms accumulated before all third-party choices were made. The owner requires third-party libraries, tools, generators, or permitted services whenever they already provide a capability. | Promote a mandatory dependency-first skill, governing convention, decision ledger, AGENTS rule, CI review surface, and automated ban on FetchContent/CPM/ExternalProject bypasses. Custom replacements now require explicit capability-specific owner authorization. |
| 2026-07-22 | `std::define_static_array` fixed allocation lifetime but GCC 16.1 still rejected a function-local non-static range as an expansion source and explicitly diagnosed the missing storage duration. | Make the promoted range `static constexpr`; if the exact GCC job still fails at this invariant, stop local reflection repair and replace the bootstrap meta implementation with Glaze's already-tested P2996 backend under the new dependency-first policy. |
| 2026-07-22 | The new dependency-policy job ran before vcpkg/build work but the minimal GCC container did not contain the guard's Node runtime. | Install the distribution-provided `nodejs` build tool in the existing frontend step; keep policy enforcement before dependency resolution and compilation so violations fail cheaply. |
| 2026-07-22 | The guard's use of `git ls-files` inherited Git's container ownership check even though the policy only needs CMake inputs. | Remove the Git dependency; walk CMake inputs with the Node standard library while excluding build, generated, vcpkg, and Git metadata trees. |
| 2026-07-22 | The local test registry duplicated a capability already provided by the owner-selected `openalgz/ut`. | Delete the custom registry/macros, package `ut` v1.2.0 through a pinned SHA-512-verified vcpkg overlay, and require its exported `ut::ut` target. The overlay passes vcpkg's enforced port checks on macOS; GCC 16.1 CI remains the authoritative suite. |
| 2026-07-22 | The initial checkout lives under a Codex-managed worktree path that is not an appropriate durable project home. | After a clean verified push, establish the canonical local checkout at `~/dev/symphony-cpp`; verify branch, HEAD, remote, and clean status there before retiring the temporary worktree. |
| 2026-07-22 | The verified `ut` checkpoint was cleanly cloned at `~/dev/symphony-cpp` on the exact pushed commit while the Codex-managed worktree remained intact. | Use the durable checkout as the canonical working copy; retire the temporary worktree only after no active task depends on it. |
| 2026-07-22 | Glaze 7.9.0 passed focused typed DTO, tolerant decode, generic object, JSON-schema, and vcpkg gates; the remaining nlohmann/json paths duplicated those capabilities. | Replace nlohmann/json with Glaze in reflected serialization/schema and Codex JSONL payloads, preserve only bounded newline framing and protocol sequencing, and guard additive fields plus invalid response identifiers with fixtures. |
| 2026-07-22 | GCC 16.1 compiled the Glaze migration, but its schema correctly emitted integral properties through `$ref`/`$defs`; the bootstrap test assumed every property carried an inline `type` and aborted on lookup. | Resolve schema property types through either inline `type` or a local `$ref` target. Keep this differential fixture so compiler-dependent type naming does not become an application contract. |
| 2026-07-22 | The owner selected OpenSymphony as the development orchestrator after comparison showed a broader operational surface than the official Elixir reference. Its Codex mode forces `danger-full-access`, disables approvals, and bypasses hook trust, while live scheduling requires Linear. | Adopt pinned OpenSymphony `v2.10.0` only as an external, container-contained development tool using its Codex harness. Keep OpenAI Symphony Draft v1 and the Elixir fixtures authoritative for C++ conformance; do not start live orchestration until the owner selects and authorizes the isolated Linear project and credential boundary. |
| 2026-07-22 | The dedicated Linear project `symphony-cpp` now exists and the owner authorized a container-scoped Codex login. OpenSymphony dry-run still requires a Linear key, and live mode can mutate active issues while its Codex child bypasses hook trust. | Add a pinned GHCR image, read-only target mount, isolated workspace and Codex-auth volumes, loopback-only control plane, one-command secret injection, and separate dry-run/live commands. Keep issues in Backlog until the image, login, and read-only dry run pass; then activate exactly one fixture-only canary. |
| 2026-07-22 | The transferred `ray-manaloto/symphony-cpp` workflow published the v2.10.0 image at manifest digest `sha256:65be3f2e87f57c9698567a3d6830ab93bd70c6268d8a36fcf1b5dad094ba6982`; contained Codex login reported ready, the read-only dry run reached a healthy control plane with zero issues, and the sole `GUI-5` fixture canary completed with zero running workers and no tracked workspace diff. | Keep OpenSymphony external and container-contained. Return the canary to Backlog after each explicit run, never infer wider Linear authority, and retain OpenAI Symphony Draft v1 plus the C++ fixture suite as conformance authority. |
| 2026-07-22 | The bootstrap scheduler accepted incomplete and adapter-blocked candidates, relied on tracker iteration order, and kept active-state runs after their adapter routability changed. | Extend the provider-neutral issue seam with `priority`, `created_at`, and `dispatchable`; reject incomplete, terminal, or unroutable candidates; sort by Draft v1 priority/creation/identifier rules; and re-check routability during reconciliation. |
| 2026-07-22 | Codex CLI 0.145.0's generated app-server schema reports per-turn and cumulative token breakdowns, while `account/rateLimits/updated` is explicitly sparse. The implementation passed the pinned GCC 16.1 Source CI run `29966368746`. | Decode the generated-schema fields into provider-neutral telemetry, accumulate scheduler totals with saturating addition, retain latest-turn usage, and merge only present rate-limit windows so sparse updates never clear known values. |
| 2026-07-22 | Retry claims were represented as running entries with only a due time, so the scheduler could neither expose Draft v1 retry metadata nor distinguish queued claims from occupied capacity. The correction passed pinned GCC 16.1 Source CI run `29966653063`. | Model explicit one-based retry entries with identifiers, monotonic due times, timer handles, and optional errors; track running workers separately; and requeue a due retry with `no available orchestrator slots` when global or normalized per-state capacity is exhausted. |
| 2026-07-22 | The Codex conversation treated one bounded read poll as a terminal timeout, so it could not honor independent one-hour turn and five-minute last-event deadlines or the specified disabled-stall behavior. The first GCC 16.1 build exposed a missing direct `<algorithm>` test include; corrected run `29967037645` passed. | Keep bounded reads as protocol polling, reset the stall clock on every decoded Codex message, enforce the total turn clock independently, return distinct stalled/timed-out outcomes so process-group teardown terminates the child, and emit distinct scheduler events before failure retry. |
| 2026-07-22 | A follow-up POSIX shell fixture for stalled-child teardown repeatedly exited before its handshake in the minimal GCC image and exposed that the bootstrap channel conflates EOF with read timeout. The protocol deadline and scheduler outcome fixtures remained deterministic. | Remove the environment-sensitive process fixture, narrow the conformance claim to directly verified deadline behavior, and require deterministic EOF/process-group lifecycle coverage in the already-selected Boost.Process v2 migration rather than expanding the custom POSIX harness. |
| 2026-07-22 | The GitHub and Linear tracker classes were empty stubs and the core issue model omitted Draft v1 fields; no portable malformed-record, pagination, profile, or error contract existed. GCC 16.1 first rejected partial aggregate test records under `-Werror`; explicit complete fixtures passed Source CI run `29968607651`. | Add a fixture-only `std::expected` tracker contract with complete domain mapping, canonical labels, fail-closed records, bounded/cycle-safe cursors, documented compact profiles, and secret-free portable errors. Keep live HTTP and mutation disabled until the Glaze HTTP gate passes. |
| 2026-07-22 | Boost.Process v2 1.91.0 from the pinned vcpkg baseline compiled on GCC 16.1 and passed fixtures for separate stdout/stderr, explicit pipe EOF, controlled working directory, terminal cancellation, and child reap in Source CI run `29969001667`. Its start-directory initializer still accepts `boost::filesystem::path`. | Promote Boost.Process v2 to the child-process lifecycle owner, confine the explicit standard-to-Boost path conversion to the adapter, and migrate the Codex runtime before adding more custom POSIX behavior. |
| 2026-07-22 | The production Codex adapter still owned `fork`/`exec`, raw pipes, polling, signals, and wait/reap after the Boost.Process gate passed. Migration first exposed the current Boost.Asio timer API under GCC 16.1; the corrected production adapter and explicit EOF fixture passed Source CI run `29969683664`. | Delete direct POSIX lifecycle ownership from the Codex adapter. Use Boost.Process v2 and Boost.Asio pipes for launch, cwd, stdio, exit request, termination, wait, and reap; retain only bounded JSONL framing and deadline policy. Keep bounded stderr diagnostics and process-tree cancellation open until directly verified. |
| 2026-07-22 | Method-bearing app-server requests were decoded as ordinary responses whenever they carried an `id`, allowing approval, user-input, and dynamic-tool requests to fall through until a timeout. The fail-closed interaction fixtures passed GCC 16.1 Source CI run `29970037383`. | Classify request methods before response IDs. Fail approval and user-input requests immediately, and return a Glaze-encoded structured failure for every unadvertised dynamic tool while continuing the turn. Keep configured startup policy payloads as a separate explicit gap. |
| 2026-07-22 | Workflow approval and sandbox settings were parsed but never reached Codex startup. Codex CLI 0.145.0 defines approval and thread sandbox as pass-through strings and turn sandbox as a tagged JSON object. | Snapshot workflow policy per run, inject it into thread/turn startup, validate raw turn-sandbox JSON with Glaze, and update it atomically on reload without maintaining duplicate Codex enums. GCC 16.1 Source CI run `29970470859` passed. |
| 2026-07-22 | Token telemetry decoded `modelContextWindow` but discarded it, and both legacy and current Codex compaction events were treated as generic notifications. | Preserve the latest reported context-window size, recognize both 0.145.0 compaction shapes, count compactions per run and globally, and emit a structured orchestration event. Leave proactive compaction thresholds, model/effort pinning, and turn-budget enforcement open rather than silently inventing policy. |
| 2026-07-22 | `agent.max_turns` was parsed and validated but every scheduler continuation launched a new app-server process and coding-agent thread. | Keep one Boost.Process-backed app-server and thread alive across bounded turns, refresh tracker eligibility after every successful turn, send continuation-only guidance, apply workflow reloads to future worker sessions, and retain the normal one-second continuation retry after the in-process loop ends. |
| 2026-07-22 | The contained Codex CLI 0.145.0 catalog advertised `gpt-5.6-sol` as the authenticated default but at `low` effort; neither worker path selected model and reasoning effort completely. | Set the single implementation worker to Sol/high: OpenSymphony receives the model through workflow routing and effort through a repository-owned, read-only, non-secret config overlay; standalone C++ passes model on thread/turn startup and Codex's `effort` on every turn. Keep authentication isolated, re-probe on image/CLI changes, and defer role-specific pools until a bounded routing contract exists. |
| 2026-07-22 | The post-pin contained OpenSymphony dry run returned `status: ok` with one Backlog fixture, zero running/retry workers, zero token usage, Codex CLI 0.145.0 readiness, and clean signal shutdown. | Accept Sol/high routing as healthy without activating another canary or starting a model-backed turn. Keep the existing one-canary authorization exhausted and require a new explicit boundary before any further live issue activation. |
| 2026-07-22 | Static Sol/high routing did not define when verified struggle should raise effort, and continuing after Codex context compaction would trust rewritten session context. | Keep work sessions to four turns; select workflow-configured Sol/xhigh after one abnormal failure or no-progress result and Sol/max only after an identical repeated failure; recover only on changed progress. After any observed compaction, stop continuation and resume from durable state in a fresh worker session. |
| 2026-07-22 | The daemon accepted only a repository-specific `--workflow` option through a handwritten loop and had no executable exit fixtures, while the normative path contract and reference runtime accept one positional workflow path. Glaze 7.9.0 exposes an interactive CLI menu, not argv parsing. | Adopt pinned CLI11 2.6.2 for strict argv parsing, standard help/diagnostics, and exit codes; support the cwd default and one positional workflow path, retain `--workflow` only as an exclusive compatibility alias, and verify help, parse failure, and startup-validation exits through the real executable. |
| 2026-07-22 | The Boost.Process adapter connected Codex stderr to a null sink, losing launch/runtime evidence and risking no bounded operator diagnostic contract. | Drain the dedicated Asio stderr pipe concurrently with JSONL stdout, retain only the final 4 KiB with saturating byte count and truncation metadata, and emit it through the structured redaction boundary. Keep process-tree cancellation as a separate Boost.Process evidence gate. |
| 2026-07-22 | Boost.Process 1.91 v2 owns the verified Asio-native lifecycle but exposes no process-group abstraction; the available group API is confined to deprecated v1 and cannot initialize a v2 process. | Do not mix lifecycle generations, replace the verified adapter, or add POSIX process-group calls without owner authorization. Retain process-tree cancellation as an explicit gap and pursue it only through a maintained cross-platform provider or an owner-approved named OS adapter. |
| 2026-07-22 | Observed-compaction rollover stopped reuse only after Codex had already rewritten context, even though cumulative tokens and the model context window were available at turn boundaries. | Add a strictly validated workflow percentage and stop continuation when cumulative usage reaches it, using overflow-safe integer arithmetic and no estimate when telemetry is absent. Configure the repository at 70% while retaining observed-compaction rollover as the fail-safe. |
| 2026-07-22 | Section 17.6 still lacked a production structured sink and direct proof that repeated cumulative Codex updates were not double-counted. The pinned registry offers spdlog 1.17.0 with synchronous multi-sink dispatch and per-sink exception interception; Quill 12.1.0 adds an asynchronous backend and queue policy not required by this boundary. | Put spdlog behind an `EventStore` adapter, retain each redacted event before sink dispatch, emit unadorned JSON to stderr, and count sink failures through a non-recursive per-logger handler. Treat token updates within one conversation as cumulative replacement, then saturating-add the final run total once at the scheduler boundary. |
| 2026-07-22 | GCC 16.1 Source CI run `29974344328` installed the new pinned spdlog dependency, then failed during CMake generation because its imported target had not been loaded. | Add the missing `find_package(spdlog CONFIG REQUIRED)` to the central dependency import list; keep the failed configure-only run as evidence and rerun the exact remote gate. |
| 2026-07-22 | Status-surface auditing exposed that `Scheduler::execute` calls `AgentRuntime::run` synchronously, so configured concurrency does not yet represent simultaneously active workers. GCC 16.1 does not document native P2300 senders/receivers, while the pinned registry packages NVIDIA stdexec commit `fee4d651494014610a277540f209cae56011e47f`. | Make real bounded worker execution higher priority than status transport. First gate stdexec on GCC 16.1 for actual overlap and value/error/stopped completion; only then migrate workers behind a seam that preserves one orchestrator mutation authority and deterministic fake time. |
| 2026-07-22 | The owner explicitly selected NVIDIA stdexec and anticipated that its vcpkg configuration might require an overlay. | Test the curated pinned port without modification first. Create a minimal same-source overlay only if exact GCC 16.1 evidence identifies a packaging-option failure; do not use an overlay to patch around an incompatible execution API. |
| 2026-07-22 | Source CI run `29974988618` configured the curated stdexec port successfully, then GCC 16.1 ICE'd in a stdexec header because the repository applied `-freflection` and CMake module scanning to the combined test target. | Do not create an overlay: the package configured correctly. Split provider and reflection fixtures into separate targets, disable module scanning for the stdexec gate, and confine P2996 flags/definitions to the reflection fixture as the architecture already requires. |
| 2026-07-22 | After separating the execution and reflection fixtures, pinned GCC 16.1 Source CI run `29975251764` configured the curated NVIDIA stdexec port, built both targets, and passed the complete test suite. | Accept the pinned registry port without an overlay. Keep stdexec behind the project execution seam and require new exact evidence before changing its packaging options. |
| 2026-07-22 | The scheduler still called one shared `AgentRuntime::run` synchronously, and that runtime exposed one global active-process cancellation slot, so configured capacity could neither overlap real workers nor cancel one safely. | Submit keyed invocation snapshots to a startup-sized NVIDIA stdexec pool, pass a cooperative stop token into each Codex conversation, and drain completion values only on the scheduler thread. Keep hooks and workspace mutation on that thread; defer terminal cleanup until the stopped worker completes. |
| 2026-07-22 | The first concurrent scheduler migration passed pinned GCC 16.1 Source CI run `29976334674`, including real two-worker overlap, scheduler-owned completion mutation, per-job cancellation, deferred terminal cleanup, and the existing suite. The slower compiler-matrix workflow still coupled tests to GHCR publication. | Keep Sol/high because the first attempt made verified progress. Make matrix image publication an explicit opt-in input and run the required GCC/clang matrix with publication disabled. |
| 2026-07-22 | The owner requires the compiler images to be reusable devcontainers and local iteration to compile only inside them. The official Dev Container CLI and CI action cover the lifecycle while Docker volumes and the mounted workspace can retain compiler caches. | Make GCC 16.1 the default devcontainer and clang-p2996 a separate differential configuration. Build the expensive pinned image layers on GitHub, validate both through the official devcontainer lifecycle, and run local configure/build/test through the thin CLI wrapper with persistent ccache, vcpkg archives, and build trees. Keep GHCR publication opt-in. |
| 2026-07-22 | The first non-publishing GCC compiler-matrix job built its image successfully, then direct command execution failed with `bash: cannot execute binary file` because the final target inherited `codex-universal`'s setup entrypoint. | Clear the inherited entrypoint in both final devcontainer targets, keep `sleep infinity` only as the direct-image default command, and require a zero-length entrypoint plus direct toolchain smoke command before the official Dev Container lifecycle gate. |
| 2026-07-23 | Adversarial workspace review found that the local executor revalidated canonical containment before hooks, but the fixture executor would accept a direct-child path swapped to a symlink after creation; corrected exact GCC 16.1 Source CI run `29981194421` passed. | Make `std::filesystem` the recorded containment mechanism, require fixture and local executors to reject traversal and symlink swaps immediately before hooks/removal, and preserve an outside sentinel in regression fixtures. |
| 2026-07-23 | The first complete clang-p2996 source build reached 4,258/4,302 targets before libc++abi rejected `LIBCXXABI_USE_LLVM_UNWINDER=ON` because the image requested libc++ and libc++abi without libunwind; compiler-matrix run `29976571206` captured the exact failure. | Build the pinned Bloomberg fork with the matched `libcxx;libcxxabi;libunwind` runtime set and keep the differential toolchain isolated from GCC 16.1 release semantics. |
| 2026-07-23 | A line-by-line audit against pinned Draft v1 §4.2 found that workspace keys discarded allowed dots, coalesced replacements, always hashed the opaque issue ID, and retained only 48 non-cryptographic suffix bits. | Preserve the normative ASCII set, replace each disallowed byte with `_`, and use the curated PicoSHA2 provider to append 128 stable SHA-256 bits from the original identifier only when replacement is required. Explicitly make the reserved `.` and `..` path components safe. |
| 2026-07-23 | Compiler-matrix run `29981978025` built and cached the GCC image, then `devcontainers/ci` failed because an image-based local config made its BuildKit extension stage pull the intentionally unpublished GHCR `edge` tag rather than consume the daemon-local tag. | Give the official action a CI-specific build-form Dev Container config and make it the sole builder, lifecycle runner, cache owner, and guarded post-success publisher. Keep image-form configs for local consumption of explicitly published images. |
| 2026-07-23 | Compiler-matrix run `29981978025` also reported that the pinned checkout, Buildx, and build-push actions still targeted deprecated Node.js 20. | Pin the owning projects' current Node 24 releases by immutable commit across every workflow; keep `devcontainers/ci` on its separately reviewed Node 24 pin. |
| 2026-07-23 | A direct audit of pinned Draft v1 §§10.1, 13.3–13.4, 17.5, and 18.1 found that the local conformance matrix incorrectly classified OPTIONAL live status wiring and owner-gated process-group hardening as core blockers. | Keep both items visible as extensions, but make required conformance depend on structured logs, the specified app-server lifecycle, deterministic restart/reconciliation fixtures, and the exact compiler evidence actually required by the implementation plan. |
| 2026-07-23 | The production Codex process inherited the complete host environment even though tracker profiles already identify their secret variables and Draft v1 requires provider credentials to stay in the host process. | Build one secret-name union from registered adapter profiles, snapshot and filter the environment before concurrent workers start, and give the result to Boost.Process v2's child-environment initializer. Preserve non-secret Codex variables and guard the boundary with a real subprocess fixture that never reads an actual credential. |
| 2026-07-23 | GCC 16.1 correctly rejected heterogeneous `std::ranges::find` test operands because `char[N]` and `std::string` do not satisfy the ranges equality concept; production tracker-secret filtering had already compiled. The typed correction passed Source CI run `29984713767`, including GCC 16.1 debug and ASan/UBSan targets. | Keep portable range fixtures type-correct instead of depending on implicit conversion. Use the exact green run together with the registered overlap, cancellation, reconciliation, and startup-cleanup cases as validation evidence while the full devcontainer compiler matrix remains outstanding. |
| 2026-07-23 | The first separate GCC 16.1 TSan gate reached project compilation in Source CI run `29985460253`, where Boost.Asio's standard fences triggered GCC's default `-Wtsan` diagnostic and the repository-wide `-Werror` made the documented instrumentation limitation fatal. | Keep TSan separate from ASan/UBSan and retain all other fatal warnings, but downgrade only GNU's `tsan` warning with `-Wno-error=tsan`. GCC 16.1 documents that ThreadSanitizer does not support `std::atomic_thread_fence` and may report false positives; preserve the warning in logs rather than disabling it. |
| 2026-07-23 | Corrected Source CI run `29985848551` retained the expected Boost.Asio fence warnings, built every GCC 16.1 TSan target, and passed `symphony_tests`, `symphony_execution_tests`, and `symphony_reflection_tests` with fatal runtime findings. | Promote the separate `gcc-tsan` preset into the source and devcontainer compiler gates. Treat the documented fence limitation as reduced instrumentation evidence around those fences, not as permission to suppress a reported race or to combine TSan with ASan/UBSan. |
| 2026-07-23 | The focused Glaze 7.9.0 YAML gate passed Source CI run `29986338063`; the production migration then passed run `29986902670`, and named repository-document validation passed debug, ASan/UBSan, and TSan in run `29987362658`. | Remove `yaml-cpp` from CMake, vcpkg, and the dependency lock. Use typed Glaze DTOs for known workflow fields, tolerant unknown-key decoding for extensions, and `generic_u64` only for opaque provider/per-state subtrees; keep expansion, paths, front-matter framing, validation, and prompt semantics repository-owned. |
| 2026-07-23 | A docs-only rerun, Source CI `29987713509`, exposed nondeterminism in `terminal reconciliation stops worker before removing workspace`: the fixture observed the worker's stop counter and called one scheduler tick before the stdexec completion value necessarily reached the drain queue. No ThreadSanitizer race report occurred. | Keep the production deferred-cleanup contract unchanged. After observing stop, tick the single scheduler authority within the existing two-second bound until it drains the keyed completion; then assert run removal and workspace cleanup. Do not weaken TSan or treat one earlier green run as stability proof. |
| 2026-07-23 | The bounded completion-drain correction passed Source CI run `29988151361` under GCC 16.1 debug, ASan/UBSan, and TSan. | Retain the explicit wait-for-drain fixture and use the documentation-only evidence commit as a second full sanitizer rerun before calling the nondeterminism corrected. |
| 2026-07-23 | Corrected build-form compiler-matrix run `29983838637` built and cached the GCC 16.1 devcontainer through the official lifecycle, then the base-image CMake frontend identified GCC 16.1 but could not map `CXX26` to a supported dialect. The pinned vcpkg tool manifest already supplies SHA-512-verified Kitware CMake 4.3.3, which Source CI uses successfully. | Acquire the exact vcpkg-owned CMake tool while seeding each final devcontainer, assert version 4.3.3, and expose that binary at `/usr/local/bin/cmake`. Keep `CMAKE_CXX_STANDARD 26` strict rather than weakening it or hard-coding compiler flags. |
| 2026-07-23 | The selected `klemens-morgenstern/sqlite` pin has no curated vcpkg port and its upstream install target omits headers and package metadata. The minimal overlay configured and linked on GCC 16.1; initial Source CI run `29991058064` then showed that the pinned typed-row converter requires SQLite's 64-bit integer type rather than `int`. Corrected run `29991779655` passed debug, ASan/UBSan, and TSan. | Accept the immutable overlay and provider gate for in-memory connection, prepared statements, RAII rollback/commit, typed reads, and non-throwing errors. Keep the provider inside the future `symphony_persistence` boundary and perform synchronous calls only on its serialized executor. |
| 2026-07-23 | Exact compiler run `29993869811` proved that pinned CMake 4.3.3 recognizes GCC 16.1's C++26 dialect and built all 82 debug targets, then CTest resolved Ubuntu's older `/lib/x86_64-linux-gnu/libstdc++.so.6` and failed on missing `GLIBCXX_3.4.35`. | Register `/opt/gcc-16.1/lib64` with the image's maintained dynamic-loader cache and make the direct image contract compile, resolve, and execute a C++ binary against that exact runtime before accepting the devcontainer. |
| 2026-07-23 | Exact compiler run `30005611193` failed its clang job before a compiler result when the hosted runner exhausted disk while entering the devcontainer build. Earlier action logs showed the old GHA scope importing a `mode=max` cache that includes the complete intermediate LLVM graph; Docker documents that `mode=max` exports every intermediate layer while `mode=min` exports only result-required layers. | Abandon the oversized scopes, use fresh versioned `mode=min` scopes for both compiler devcontainers, and bound cache export to 30 minutes. Treat running partial logs as non-authoritative hints and never cancel from their staleness; only explicit job state or the configured timeout may terminate a monitored run. |
| 2026-07-23 | The first `symphony_persistence` slice put the pinned SQLite provider behind an explicit versioned event envelope and a one-thread NVIDIA stdexec pool. Initial Source CI run `30025001023` reached the new target and rejected one redundant move under GCC 16.1 `-Werror`; corrected exact run `30025453859` passed debug, ASan/UBSan, and TSan in 6m27s, including restart, invalid-envelope, and concurrent-append fixtures. | Keep Boost.SQLite and stdexec types private to the adapter, preserve ordered repository-owned SQL and error types, and do not wire persistence into the scheduler until a cancellable queued submission boundary is fixture-proven. Run the recorded `sqlgen` deletion-test before adding repetitive domain mappers. |
| 2026-07-23 | The bounded pinned-registry sqlgen 0.6.0 deletion test found that its documented write path creates tables from reflected structs, aliases errors through `rfl::Result`, and adds reflect-cpp, CTRE, and yyjson while remaining synchronous. It would replace the explicit migration boundary with a second reflection/result authority and would not supply the missing cancellation handoff. | Reject sqlgen for the durable event repository before adding it to the manifest. Retain explicit migrations and thin prepared mappings over the selected SQLite provider; re-evaluate only if a future release supports manual schema authority and deletes measured mapper code without duplicating Glaze or `std::expected`. |
| 2026-07-23 | Cancellable asynchronous SQLite appends passed exact GCC 16.1 Source CI run `30029693310` under debug, ASan/UBSan, and TSan. The next fixture-only run `30030352997` then failed at compile time as intended because the repository had no configured queue bound, typed overload result, or admission-returning submission API. The first correction run `30030849744` reached the implementation and showed that GCC 16.1's library does not yet provide the C++23 `<scope>` header; corrected run `30031349935` passed debug, ASan/UBSan, and TSan in 8m37s. | Keep NVIDIA stdexec as the queued execution provider. Apply a startup-fixed repository admission limit before submission, return `resource_unavailable_try_again` through the repository-owned overload category, and explicitly release the slot after normal completion or submission failure instead of adding a local scope-guard replacement. Prove that rejected work never reaches SQLite and capacity is reusable after drain. |
| 2026-07-23 | Nonpublishing compiler-matrix run `30016340827` proved the GCC 16.1 devcontainer in 58m54s. Its clang-p2996 job reached project compilation after building the pinned toolchain, then the fork asserted in Glaze's fallback field-name instantiation in three ordinary translation units; an independent unused-private-field warning also remained fatal. | Treat GCC 16.1 as executable semantics and keep clang-p2996 differential-only. Narrow the clang milestone to the isolated P2996 adapter/fixture boundary before another expensive matrix; do not mask the compiler assertion, broaden experimental flags, publish an image, or block contained GCC development on the fork defect. |
| 2026-07-23 | The first isolated P2996 target reached GCC 16.1 scanning in Source CI run `30032098786`, then failed because it linked only compile options and `ut`, so the new header-only adapter did not propagate the repository include root. Corrected run `30032552045` passed debug, ASan/UBSan, and TSan in 7m20s. | Represent the dependency-free adapter as an explicit `symphony_reflection` interface target, make `symphony_meta` and the differential fixture consume that target, and keep Glaze outside the clang-only target graph. |
| 2026-07-23 | Consecutive small Source CI slices spent roughly four minutes recreating the same pinned vcpkg packages before project compilation, and ccache state disappeared with each hosted container. Current primary sources identify `actions/cache` 5.0.5 as the Node 24 cache transport and vcpkg's default filesystem archive as its maintained local binary cache. | Pin the cache action by exact commit, key vcpkg archives by compiler plus dependency inputs, restore a per-commit GCC ccache lineage capped at 750 MiB, and keep both under the already ignored `.build` tree. Cache misses remain non-fatal and never skip configure, build, or tests. |
| 2026-07-23 | The first cold-cache Source CI run `30033135155` reached configure in 41 seconds, where vcpkg correctly rejected the configured archive path because a cache miss does not create that directory. Corrected cold run `30033259028` passed debug, ASan/UBSan, and TSan in 8m02s, and both pinned cache post-actions completed successfully. Warm run `30033915395` restored the caches and reached the intentional debug fixture failure in 1m33s. | Create the pinned vcpkg archive directory explicitly after cache restore and before configure; preserve cold misses as a supported path rather than requiring a pre-existing cache entry. Use the measured warm red-cycle time only for inner-loop evidence; require a later complete warm run before comparing full-suite duration. |
| 2026-07-23 | Warm Source CI run `30033915395` passed every existing debug target, then the new migration fixture failed only because a fresh database retained `user_version=0` and a database marked version 2 was accepted. Corrected run `30034126584` passed debug, ASan/UBSan, and TSan in 3m23s; legacy-row preservation passed exact run `30034661951` in 3m27s. | Read the schema level before connection-mode changes, reject versions newer than the executable, migrate version 0 to 1 transactionally, validate the expected event-table shape on every successful open, and retain version-0 event rows during the upgrade. |
| 2026-07-23 | Warm Source CI run `30035056060` reached the durable-event fixture in 1m06s and failed only because the specified adapter/header did not yet exist. | Add a thin fanout adapter that redacts once before both sinks, delegates bounded history/logging, submits versioned JSON to the existing asynchronous repository channel, counts overload and completion failures, and drains accepted work at shutdown. Keep that repository completion channel exclusive to the adapter until a later partitioned-completion contract is fixture-proven. |
| 2026-07-23 | The first durable-event implementation run `30035233374` reached project compilation in 1m40s, where GCC 16.1 rejected one partially initialized `observability::Event` aggregate under the existing `-Werror=missing-field-initializers` policy. This repeated an earlier complete-fixture lesson. | Fully initialize every fixture field. Classify partial aggregate fixtures as a recurrent compile-contract family and promote public-header/self-contained compile gates plus selected static analysis rather than weakening warnings. |
| 2026-07-23 | Corrected exact Source CI run `30035446759` passed GCC 16.1 debug, ASan/UBSan, and TSan in 3m35s for the redacted durable-event fanout. Local devcontainer validation could not start because the intentionally unpublished `ghcr.io/ray-manaloto/symphony-dev:edge` tag does not yet exist. | Accept the durable-event slice. Keep Mac compilation disabled; use the warm GCC source container until a healthy compiler matrix supports the separately authorized GHCR publication ceremony. Do not start a duplicate compiler build merely to unblock one focused edit. |
| 2026-07-23 | The stopping-point engineering review found that library selection is coherent, but repeated failures were recorded without a general recurrence/promotion contract; parallel roles lacked file/resource claims; and formatting, static analysis, API documentation, site generation, and diagram validation had no pinned gates. A system compaction during the review preserved repository and native-goal state but confirmed that durable checkpoints must precede context pressure. | Add a maintained C++ ecosystem catalog and an engineering-system policy with 50/60/70% context thresholds, mandatory fresh sessions after compaction, normalized failure families, second-occurrence guard promotion, third-occurrence Human Review, read-only parallel specialists, serialized integration, and phased stock-Clang/MrDocs-or-Doxygen/MkDocs/Graphviz evaluation. Keep OpenSymphony concurrency at one until write-lane isolation is fixture-proven. |
| 2026-07-23 | Glaze event serialization initially passed ordinary text but exact Source CI run `30037043198` showed that its default options do not escape every JSON control character. Corrected run `30037312084` passed GCC 16.1 debug, ASan/UBSan, and TSan in 3m40s. | Keep Glaze as the sole event JSON provider, select its `escape_control_characters` option at the structured-event boundary, and retain a round-trip fixture for every control byte rather than restoring a custom escape routine. |
| 2026-07-23 | The first compile-time field-descriptor fixture failed as specified in Source CI run `30037676397` because the adapter returned allocating `std::vector<std::string>` state. Exact corrected run `30037859345` passed all GCC 16.1 source-container configurations in 3m23s. | Return a `consteval` fixed `std::array` of `std::string_view` descriptors from the isolated P2996 adapter and require compile-time field-name/type assertions in both GCC and clang differential fixtures. |
| 2026-07-23 | The owner directed use of the supplied Intel libraries at latest immutable Git commits with continuous reviewed updates when no curated vcpkg port exists. The current Intel repositories have no install/package configuration, invoke prohibited CPM from upstream CMake, advertise GCC 12-14 rather than GCC 16.1, and internally test older cross-repository pins. | Add direct-install vcpkg overlays for the atomic latest-main Intel closure, record commit plus SHA-512 locks, and gate it with a separate GCC 16.1 provider fixture. Use CIB/`stdx` to compose tracker and Codex dispatch validators behind the existing API; keep the Intel bare-metal executor out of product scheduling because NVIDIA stdexec remains the selected hosted provider. Follow with a scheduled reviewable pin updater rather than floating build refs. |
| 2026-07-23 | Kitware released CMake 4.4.0 while the development images still asserted 4.3.3. Official CMake 4.4 adds Presets schema 12, configure warning/error controls, stable `${fileDir}` include behavior, and test argument forwarding; the existing project already requested strict C++26 and compiler-specific reflection activation. | Pin the official 4.4.0 Linux AMD64 archive and checksum in one installer used by Source CI and both devcontainer images. Raise the project/preset minimum to 4.4, adopt schema 12 warning controls, explicit build/test parallelism, no-tests errors, and workflow presets while preserving the authoritative GCC and differential clang boundaries. |
| 2026-07-23 | The compiler audit confirmed strict `-std=c++26`, but CMake's module scanner remained enabled for most targets even though the pinned reflection fork documents module/PCH incompatibilities. The clang differential path also used the broad `-freflection-latest` umbrella, and configure did not prove the exact reflection facilities before a long build. | Disable module scanning globally until an explicit modules slice is approved; express `cxx_std_26` transitively; probe `<meta>`, field enumeration, `define_static_array`, and expansion statements during configure; use GCC `-freflection` and clang `-freflection -fexpansion-statements`; keep differential clang warnings non-fatal while GCC owns `-Werror`. Add format/shadow/undef/virtual warnings, `_GLIBCXX_ASSERTIONS` in GCC Debug, and non-recovering ASan/UBSan. |
| 2026-07-23 | The compiler devcontainer seed used `COPY . .` before its vcpkg install, so every source or documentation edit invalidated the dependency layer even when the manifest, overlays, triplets, and bootstrap pin were unchanged. The cppalliance local CI design reinforces separating reusable toolchain/dependency state from the mounted source tree, while prior run `30005611193` already proved that exporting the full intermediate compiler graph exceeds the hosted runner's disk budget. | Copy only vcpkg dependency inputs into the seed stage and retain the fresh compiler-specific BuildKit `mode=min` scopes. Keep ccache as the sole compiler-result cache, avoid redundant sccache, and leave local source/build trees mounted into the published devcontainer rather than baking them into the reusable image. |
| 2026-07-23 | Exact Source CI runs `30040027088` and `30040593648` ICEd GCC 16.1 at Intel `stdx::tuple` while instantiating CIB callback/nexus composition. The second run removed all module-scanner flags; a standalone exact-image probe then reproduced the ICE with modules on/off and C++23/C++26, while CIB's tuple-free lookup surface compiled and ran. | Keep GCC 16.1 authoritative and do not patch broad upstream tuple machinery. Restore direct product-specific dispatch validation, expose the overlay's narrow `cib::lookup` target, and replace exact tracker HTTP-status branches with a consteval lookup plus known/default static assertions. Retain Intel bare-metal libraries only in isolated compatibility fixtures and NVIDIA stdexec as the sole hosted executor. Re-evaluate nexus after a future immutable pin passes standalone GCC tuple/nexus probes. |
| 2026-07-23 | The owner requires non-curated libraries to track latest immutable commits through continuous reviewed updates. Two legacy overlays still exposed `HEAD_REF`, `openalgz/ut` was behind `main`, and the existing policy scan did not prove that portfiles, manifests, and lock rows agreed. The maintained create-pull-request action was evaluated but its update path uses force-with-lease, which conflicts with repository publication policy. | Allowlist six overlay repositories/branches, update `ut` to current `main`, remove every `HEAD_REF`, and make the dependency policy invoke an offline exact-pin/hash/version/lock checker. Run a weekly/manual resolver that stops for curated-port migration, validates changed graphs through exact GCC 16.1 Debug, ASan/UBSan, and TSan, then uses GitHub CLI to make one run-unique ordinary push and draft PR only when no earlier updater PR is open. Manual runs remain validation-only by default. |
| 2026-07-23 | OpenSymphony image workflow run `29983539065` republished mutable `edge` at manifest `sha256:56d7fd2274e2d7eda4ff1d3556a6a9315014e3a05055422e9c4c68849be36911` after workflow-action pinning and passed tool smoke tests. The completed contained login, read-only dry run, and sole `GUI-5` fixture canary had used manifest `sha256:65be3f2e87f57c9698567a3d6830ab93bd70c6268d8a36fcf1b5dad094ba6982`; allowing mutable `edge` to replace it would overstate validation and make the runner non-reproducible. | Default the launcher to the ceremony-proven digest and record the newer edge digest as smoke-only. Preserve `OPENSYMPHONY_IMAGE` solely as an explicit re-validation override. Do not repeat the exhausted canary authorization or infer new Linear authority from a workflow rebuild. |
| 2026-07-23 | Formatting and static analysis had no reproducible tool owner, while using the experimental reflection fork for either would conflate differential coverage with source-quality policy. LLVM 22.1.8 is the final planned 22.1.x release and publishes a signed, attested Linux X64 tool archive; its tag resolves to commit `ca7933e47d3a3451d81e72ac174dcb5aa28b59d1`. | Add a third, reflection-disabled analysis devcontainer from the official SHA-256- and signature-verified archive. Keep GCC 16.1 authoritative and clang-p2996 differential-only. Make exact clang-format fatal, begin with an explicit narrow clang-tidy report, exclude generated/third-party code, and promote checks only after the project-owned baseline is zero. |
| 2026-07-23 | Compiler-matrix run `30033172998` built more than 4,080 of 4,302 LLVM targets over 2h34m, then failed while importing the all-tools clang-p2996 image with `no space left on device`. GCC 16.1 passed, and the clang failure occurred during image export rather than source compilation or reflection testing. | Use the pinned fork's maintained LLVM distribution mechanism: build and strip only `clang`, its resource headers, `lld`, and the enabled `libcxx;libcxxabi;libunwind` runtime umbrella through `install-distribution-stripped`. Disable upstream tests, examples, benchmarks, and docs; assert the installed compiler, linker, and `<meta>` header before discarding the builder. Never repeat an all-tools `install` target for the differential image. |
| 2026-07-23 | The CMake 4.4 preset audit found that `gcc-release` configured and built optimized code but had no test preset, workflow preset, CI step, or local devcontainer entry point, despite the engineering policy requiring optimized diagnostic coverage. | Add one `gcc-release` configure/build/test workflow with no-tests-as-error, run it in Source CI and the GCC devcontainer matrix, and expose the same exact path through the local devcontainer wrapper. Keep sanitizer configurations separate. |
| 2026-07-23 | The first full OpenSymphony acceptance review found that environment-selected test volumes could alias and delete retained auth or memory state, workspaces still used the operational volume, the read-only MCP check did not prove rejected mutation, and `init` followed a mutable template branch. | Require exact suffix-scoped acceptance volume names, reject every pre-existing Docker resource, clean up only resources created by the test, isolate workspaces, mount MCP memory read-only and fingerprint it around a rejected admin call, and pin disposable `init` to an immutable template commit and tree. Keep operational volumes and the canonical checkout outside automated acceptance. |
| 2026-07-23 | Full pinned OpenSymphony workspace tests reached 45 passing memory cases, then `memory_import_okf_mid_copy_failure_documents_partial_state` unexpectedly succeeded because the container builder ran as root and bypassed the fixture's `0555` directory boundary. | Compile and test the exact upstream workspace as uid/gid 10001 with a private Cargo home, then elevate only to copy the already-built artifacts into the image. Keep the upstream permission-failure case in the required full-workspace gate; never skip it or weaken the suite. |
| 2026-07-23 | The compiler matrix used `devcontainers/ci` to build, run, and potentially publish complete repository devcontainers, coupling expensive compiler construction to vcpkg seed layers and local lifecycle configuration. The owner clarified that CI should publish stable toolchain bases while the Dev Container CLI builds the daily environment locally. | Split the supply chain: Buildx constructs and directly tests repo-independent GCC, clang-p2996, and LLVM-analysis base images; local immutable-digest Dockerfiles plus the reference CLI will own workspace mounts, persistent caches, lifecycle setup, and editor configuration after the first base digests are published and reviewed. Cancel the in-flight publish-enabled matrix before it can publish the obsolete artifact, retain `mode=min` lineage caches, pin the CLI version, and document the workflow as Mermaid diagram source. |
| 2026-07-23 | The owner clarified that OpenSymphony is a local orchestration control plane, not a devcontainer tool or a CI-published artifact. OpenSymphony workflow run `30049554673` was still compiling and had not reached publication when canceled. | Delete the OpenSymphony publication workflow. Build a separate local-only orchestrator image from the same immutable GCC base as the C++ devcontainer, while keeping OpenSymphony itself out of that devcontainer. Run the pinned upstream Rust tests in a builder stage, preserve contained volumes and mounts, and repeat acceptance/doctor/read-only dry-run locally before treating a new image as healthy. |
| 2026-07-23 | Full v2.10.0 inventory showed that `run --dry-run` is no-model but still creates or recovers workspaces, writes manifests, and runs hooks; `doctor` can create directories and manage OpenHands tools; the top-level `codex:` policy map and `agent.max_turns` are not enforced; and gateway capability flags overstate several wired controls. Upstream issues [#222](https://github.com/kumanday/OpenSymphony/issues/222), [#223](https://github.com/kumanday/OpenSymphony/issues/223), and [#224](https://github.com/kumanday/OpenSymphony/issues/224) now track those gaps. | Prove every shipped OpenSymphony and requested standalone OpenHands GUI feature in contained fixtures before disabling any. Use disposable repositories plus fake Linear, GitHub, and model endpoints for destructive paths; retain `GUI-5` as the sole live canary. Add an external local C++26 supervisor around the stock process with four model-backed runs, a 90-minute deadline, a pause after two consecutive no-progress or repeated-failure results, and 50/60/65% checkpoint/handoff/rollover boundaries. Keep plan-conformant ordinary changes eligible for separately credentialed autonomous merge, but require researched human resolution for ambiguity, specification conflicts, dependency/toolchain-pin policy changes, or stale/incomplete evidence. |
| 2026-07-23 | Project-scoped Docker cleanup reclaimed 341.62 GB of unused and dangling image data while preserving the active GCC devcontainer, retained auth/state/workspace volumes, compiler caches, and the ceremony-proven OpenSymphony fallback image. The remaining 15-day-old Docker Desktop Kind cluster had no repository consumer; the owner authorized its deletion. | Disable Docker Desktop Kubernetes, wait for the control-plane and volumes to disappear, and remove the empty `kind` network. Keep future cleanup exact-resource and age scoped; never prune active project caches or unrelated Docker resources. |
| 2026-07-23 | OpenSymphony v2.10.0 does not enforce the repository's model-effort, lifetime, no-progress, retry-recurrence, or context policy. A pure C++26 supervisor reducer initially used total tokens, and its added differential fixture correctly failed because cached/output/reasoning tokens are not context utilization. GCC 16.1 then passed 7 tests and 30 assertions after switching to last-turn input tokens. | Keep stock OpenSymphony as the scheduler and put deterministic budgets around its process. Checkpoint at 50%, hand off at 60%, roll over at 65%, roll over after compaction or four completed turns, and checkpoint rather than estimate when the context window is missing. Persist progress/failure evidence across every session boundary. |
| 2026-07-23 | The GCC 16.1 devcontainer exposed two independent fixture-platform issues: the stdexec overlap test observed runtime return before the completion callback was enqueued, and Boost.Process v2 selected `pidfd_open` even though Docker Desktop returned an unusable descriptor. The pinned Boost 1.91 headers provide a supported `BOOST_PROCESS_V2_DISABLE_PIDFD_OPEN` SIGCHLD/`waitpid` fallback. | Make the overlap fixture hold worker return and boundedly drain nonblocking scheduler ticks. Select the Boost fallback consistently for every Linux translation unit and guard it at compile time; five process fixtures passed three consecutive focused runs. |
| 2026-07-23 | After the supported Boost.Process fallback removed the container abort, a slow-start fixture showed that the Codex conversation charged initialization and thread creation against the turn deadline. Its environment-isolation shell checks also continued after failure. A delayed-handshake regression failed before the correction; the complete GCC 16.1 suite then passed all nine CTest targets. | Start the independent turn clock only after the initial `turn/start`, keep stall/startup clocks separate, and make environment assertions fail closed. Preserve the real one-second post-start timeout fixture while allowing bounded emulated process startup. |
| 2026-07-23 | Compiler-matrix run `30052155024` passed GCC 16.1 and clang-p2996 but failed the LLVM analysis image. Targeted run `30054664305` proved that archive download, signature/hash verification, extraction, and the pinned LLVM commit all succeeded; only exact equality against `clang-format version 22.1.8` failed because the official binary appends its repository and commit provenance. | Centralize the clang-format version check. Accept only the bare pinned version or the exact official `llvm-project` URL plus pinned commit; reject different versions, commits, and arbitrary suffixes. Keep this contract in a source fixture before the next guarded compiler-matrix dispatch. |
| 2026-07-23 | The first cached, non-root OpenSymphony image rebuild passed all 848 library tests and the earlier load-sensitive keepalive case, then failed 1 of 72 gateway cases because `gateway_code_graph_bootstraps_empty_store_and_dirty_workspace_flow` permits only 200 50-millisecond polls. A neighboring code-graph gateway case took about 150 seconds and passed under AMD64-on-Apple-Silicon emulation. Upstream issue [#225](https://github.com/kumanday/OpenSymphony/issues/225) records the nondeterministic fixed deadline. | Keep compile and test layers separate so compilation remains reusable. Preserve the complete pinned upstream suite, run Rust cases serially in the emulated fallback build to remove intra-binary CPU contention, and retain the test's finite fail-closed deadline. Prefer native ARM64 bases once the compiler-image supply chain publishes them. |
| 2026-07-23 | Static acceptance review found that the existing script was a partial Codex/memory/demo/`init` smoke, not the documented no-model or full-feature acceptance; it retained outbound access, mounted the live workflow, exposed a forwarded Linear key in Docker metadata, lacked uniform resource limits, and allowed bare `dry-run` to reuse operational volumes. | Label the smoke accurately, put fixture service containers on an internal network, stream live keys over stdin, require exact suffix-scoped dry-run volumes, apply bounded CPU/memory/PID/capability controls, and add an upstream-compatible fake Linear service that rejects every mutation and logs only operation names. Keep the rest of the full v2.10.0 feature matrix as explicit disposable-fixture gates rather than claiming premature coverage. |
| 2026-07-23 | Native-architecture research found no intrinsic AMD64 requirement: GCC 16.1 supports AArch64 and C++26 reflection, codex-universal is multi-architecture, and official CMake 4.4.0 and LLVM 22.1.8 ARM64 assets exist. The current AMD64-only pins, X86 LLVM backend, runtime checks, and cache/build paths are supply-chain choices; the GCC reflection patch's published bootstrap evidence remains x86-64. | Build each lineage on native GitHub AMD64 and ARM64 runners, isolate every cache/build/vcpkg path by architecture, validate both child images before assembling a reviewed GHCR index, use ARM64 for Apple Silicon daily development, and retain explicit AMD64 executable-semantics parity. Promote ARM64 reflection only after the complete GCC workflows pass; extend clang-p2996 afterward as differential-only. |
| 2026-07-23 | The serial OpenSymphony v2.10.0 retry passed the previously failing 57-case gateway suite, including the slow code-graph contract case, then reached 45 of 46 passing memory integration tests before `memory_capture_discovers_github_by_default_and_reports_missing_gh` failed its hidden stderr assertion. The non-root builder and pinned Rust base do not provide `gh`; the assertion omits the observed stderr, and a focused diagnostic would repeat the discarded 39-minute emulated compile. Upstream issue [#226](https://github.com/kumanday/OpenSymphony/issues/226) requests a hermetic missing-command fixture and actionable diagnostics. | Stop the retry loop and keep the full pinned workspace suite as a distinct `upstream-tests` image target. Build the local release in an independent non-root target so contained feature acceptance can continue, but do not classify the image as healthy or enable a live issue until the upstream suite passes. Preserve the exact failure as upstream evidence and prioritize native ARM64 supply-chain support. |
| 2026-07-23 | Publication review found that missing or invalid context telemetry returned `checkpoint_then_continue`, contradicting the supervisor's documented fail-closed pause. The changed fixture first failed all four missing/absent/zero/negative-window cases under GCC 16.1; after adding a distinct `pause_needs_telemetry` action, the focused 7-test/30-assertion target and the complete nine-target GCC debug CTest suite passed. | Keep missing telemetry distinct from a normal 50% checkpoint. Never estimate a context window or authorize another run until positive telemetry is restored and reconciled. |
| 2026-07-23 | A third static acceptance audit found that name-only disposable volumes could be adopted or deleted after a race, arbitrary fake GraphQL queries were accepted, candidate images could be mistaken for fully tested images, and the README could authorize a second live canary. The corrected scripts now verify per-run labels after volume/network creation, at launcher admission, and immediately before cleanup; label named containers; allowlist only the two required Linear query operations; prove foreign-token rejection; and separate an explicitly unvalidated candidate target from the normal image target that descends from the complete upstream suite. The reviewer's delta audit and all lightweight fixture/launcher tests passed. | Use immutable local image IDs in evidence. Permit the candidate only for contained diagnostics while upstream #226 is open; only the test-descended image may carry `dev.opensymphony.upstream-tests=passed` or the operational `:local` tag. Preserve `GUI-5` as the already-consumed sole live canary and require new authorization for any later activation. |
| 2026-07-23 | Contained acceptance of candidate image `sha256:637fc465b0113ac4145e909cc2937b04eba4eddf8f5b5f8c17f7875842501274` exposed three integration-contract mistakes before passing: the current no-model snapshot omits `codex_thread_id` instead of emitting `null`; standalone memory commands use the process working directory as the repository root and interpret `memory --config` as the memory-policy YAML, not the orchestrator runtime YAML; and FrankenTUI requires an allocated pseudo-terminal. The corrected gate passed fixture-only Codex routing with zero conversations, turns, and tokens; mutation-denying fake Linear; lifecycle hooks and run manifest; private memory capsule, DuckDB index, docs sync, read-only MCP authorization; demo daemon/TUI attachment; immutable template initialization; and owner-token cleanup with no retained acceptance resources. | Keep `/target` as the contained process working directory, pass `/target/.opensymphony/memory/memory.yaml` to every standalone memory subcommand, treat an absent thread identifier plus zero conversation count as the no-model contract, and allocate a bounded Docker TTY only for FrankenTUI. This proves the candidate integration slice, not upstream health: retain the unverified label and do not promote or activate another canary until the full pinned suite passes after upstream #226 is resolved. |
| 2026-07-23 | The green initial no-model acceptance created a workspace but its always-active fake Linear response could not exercise OpenSymphony's startup terminal-recovery path or `before_remove`. New terminal-mode expectations failed against that fixture before its implementation. | Give the mutation-denying fixture immutable `active` and `terminal` startup modes rather than a state-changing control endpoint. Stop the first dry-run scheduler, preserve and fingerprint its disposable workspace artifacts, restart against the same issue in `Done`, and require recovery to add one `before_remove` marker without changing the prior manifest/bootstrap artifacts or creating a conversation, thread, turn, token usage, or tracker mutation. Keep a fresh complete candidate acceptance as the evidence gate; static and fake-service tests alone do not mark recovery passed. |
| 2026-07-23 | Fresh complete acceptance of immutable candidate `sha256:637fc465b0113ac4145e909cc2937b04eba4eddf8f5b5f8c17f7875842501274` initially mistook `doctor`'s prepared `/workspaces/symphony-cpp` directory for a second issue execution and expected issue-level `turn_count` after terminal recovery had correctly removed the issue from the snapshot. The corrected evidence gate found exactly one run manifest, retained its successful no-model hook record and bootstrap fingerprints, observed `issues: []` with zero retry depth after recovery, ran `before_remove` exactly once, and passed the complete Codex-route, memory/MCP, daemon/TUI, and immutable-init acceptance with no model conversation, thread, token use, tracker mutation, or retained acceptance resource. | Treat run manifests, not top-level preparation directories, as the issue-execution cardinality boundary. Assert zero turns while the issue record exists, then assert empty active issues plus zero tokens/conversations and absent thread identity after terminal recovery. Keep this candidate diagnostic-only while upstream #226 prevents the full test-descended `:local` image. |
| 2026-07-23 | The first read-only AST acceptance attempt nested a temporary fixture mount below the read-only `/target` bind, and the next attempt let the memory daemon inherit the image's `/orchestrator` working directory, so relative paths resolved outside the mounted repository. After mounting the fixture over the existing repository directory and setting the daemon working directory to `/target`, candidate `sha256:637fc465b0113ac4145e909cc2937b04eba4eddf8f5b5f8c17f7875842501274` passed the complete contained acceptance with ten supported AST/lightweight documents, exact provider/parser/query-pack provenance, SHA-256 citations, zero diagnostics, explicit C++ exclusion, unchanged memory state, and no retained resources. | Mark only the read-only AST query-pack sub-gate proven. Keep persistent code-graph ingestion, graph-backed context, and C++ parsing open; preserve the repository-root working directory and existing-mountpoint rule in contained memory-server launches. The candidate remains diagnostic-only while upstream #226 blocks a fully test-descended image. |
| 2026-07-23 | Workflow `max_concurrent_agents`, `max_turns`, and per-state concurrency were decoded losslessly as `uint64_t` but then narrowed to `uint32_t` without a representability check. A GCC 16.1 devcontainer regression failed for all three `UINT32_MAX + 1` fixtures; after applying standard `std::in_range`, the focused workflow test and all nine GCC Debug CTest targets passed. The exact LLVM 22.1.8 analysis devcontainer could not start because its configured GHCR `edge` base is not published, while the validation-only compiler matrix deliberately publishes nothing. | Keep standard-library representability checks at the workflow/domain boundary and leave Intel Safe Arithmetic deferred under its non-production upstream status. Do not substitute the host's LLVM 20 formatter for the pinned LLVM 22.1.8 gate; resolve the analysis-base digest/publication boundary through the compiler-image supply chain. |
| 2026-07-23 | The first native-architecture implementation slice replaced the AMD64 Codex child pin with its immutable multi-platform index, mapped official CMake 4.4.0 and LLVM 22.1.8 archives and checksums by target architecture, selected the clang-p2996 X86/AArch64 backend from BuildKit `TARGETARCH`, and removed the x86-specific `ldconfig` presentation check. The offline selector fixture, dependency policy, shell syntax, and Buildx Dockerfile check passed with no warnings. | Keep this as architecture-selection evidence only. The current AMD64 validation matrix remains the baseline; native ARM64 runner fan-out, architecture-scoped caches, complete GCC reflection/source workflows, and reviewed multi-platform publication must pass before an ARM64 image or local devcontainer becomes operational. |
| 2026-07-23 | GitHub evaluates job-level conditions before matrix expansion, and the native ARM runner has only 14 GB of disk, so enabling all six lineage/architecture cells at once would obscure first-failure attribution and risk repeating expensive builds. | Add a typed one-architecture dispatch defaulting to AMD64. Qualify ARM64 first with `lineage=gcc16`, a native `ubuntu-24.04-arm` runner, architecture-specific tags/cache paths/keys/Buildx scopes, preflight capacity evidence, and the complete GCC reflection/source workflows. Reject every publication request and keep the publication job fail-closed until the same commit passes native AMD64 and ARM64 and the two-child index ceremony is separately reviewed. |
| 2026-07-24 | Validation-only compiler run `30067119954` finished constructing the LLVM analysis base, then `load: true` duplicated its Codex/GCC/LLVM layers into Docker Engine and exhausted the runner while writing an unrelated inherited Rust LLVM library. GCC 16.1 passed in 44m20s. The clang-p2996 job was initially recorded as passing in 1h37m52s, but later log review after run `30097575001` proved its pinned `ut` install and root CMake configure had both failed; the old multiline shell lacked fail-fast propagation and a final successful `ccache --show-stats` masked them. | Derive ephemeral validation stages from each publishable base, run tool/runtime and bind-mounted source contracts inside BuildKit, and use Docker's `type=cacheonly` exporter with `load: false` and `push: false`. Keep every validation command joined by `&&` or executed under explicit fail-fast shell semantics so a reporting command cannot overwrite a prior failure. Keep `mode=min` lineage caches; bridge only bounded ccache through pinned cache-dance/actions-cache because ordinary GHA export does not persist cache-mount contents. Remove the legacy publication job until a separately reviewed ceremony directly pushes architecture-matched base targets and records child digests without local image loads. |
| 2026-07-24 | The first Codex startup DTO reflection fixture failed before implementation because the private protocol header did not exist. The first full GCC build then caught an overly broad source-root include assumption that the isolated target masked, and independent reviews found that name/order-only checks would miss type and optionality changes and that reflection-disabled configurations could register a vacuous test. | Extract only the five startup wire DTOs into a source-private internal header. Under GCC 16.1 with `-std=c++26 -freflection`, assert exact member names, declaration order, types, and optionality at compile time; register the fixture only when reflection is enabled; and retain public Glaze frame tests for exact key sets, omitted optionals, IDs, input shape, and raw sandbox JSON. Keep this GCC-authoritative because the clang-p2996 differential seam deliberately excludes Glaze, and leave workflow DTOs for a separate bounded slice. |
| 2026-07-24 | Running the new local devcontainer lifecycle against an existing dotfiles-enabled container proved that `mise install --locked` also loaded the developer's global mise config and failed on unrelated Java, Go, Ruby, and other tools absent from the repository lock. The five project tools installed, but the fail-fast hook installation never ran. | Set `MISE_GLOBAL_CONFIG_FILE=/dev/null` for project trust, locked installation, and hook setup. This preserves mounted dotfiles for interactive use while making repository setup depend only on `.mise.toml` and `mise.lock`; rerunning the isolated command installed both pre-commit and pre-push hooks successfully. |
| 2026-07-24 | The installed pre-push hook unconditionally invoked Docker Buildx, but the GCC devcontainer deliberately exposes neither the host Docker socket nor Docker CLI. A push from the intended day-to-day environment would run the repository checks and then fail before Docker static checks and push completion. | Give hooks an adaptive `pre-push` mode: always run the exact-version lint, workflow, schema, security, dependency, platform, and JSON checks, then run Buildx static target checks only when Docker is available. Keep explicit `docker` mode fail-closed for the host-side guarded publication ceremony. Do not mount the Docker socket merely to satisfy a hook. |
| 2026-07-24 | Planning the workflow DTO reflection contract exposed a masked configuration mismatch: the shared repository workflow put `stall_timeout_ms` only under `agent`. Pinned [Draft v1](https://github.com/openai/symphony/blob/1f3219bb1ea5f69a1305dc594e79b0db57c113c5/SPEC.md#536-codex-object) defines `codex.stall_timeout_ms`, while pinned [OpenSymphony v2.10.0](https://github.com/kumanday/OpenSymphony/blob/0cc21ddda5d1853a8fbd11add578b43b6ebd6fcb/crates/opensymphony-workflow/src/model.rs#L104-L111) uses `agent.stall_timeout_ms`; the identical five-minute default hid the standalone omission. | Keep both explicitly in the shared workflow and label the `agent` key as comparative. Extract only the seven raw workflow decode DTOs into a source-private header, retain Glaze metadata in the production translation unit, and use GCC 16.1 C++26 reflection to assert every known decoded field's name, declaration order, type, and optionality. Public loader fixtures remain the codec authority and cover the previously unasserted hook, retry, sandbox, and timeout mappings. |
| 2026-07-24 | Cache-only LLVM-analysis run `30073519674` installed every pinned dependency, then failed after 35 minutes because BuildKit's bind source is the `.dockerignore`-filtered context and intentionally contains no `.git`, while `check-format.sh` required `git ls-files`. This was a source-enumeration contract defect, not an LLVM, compiler, disk, or image-export failure. | Centralize formatter source discovery across all C/C++ source and header extensions under `include`, `src`, and `tests`. Preserve `git ls-files` in real checkouts so ignored/generated files stay excluded, and use the same extension boundary through `find` when validating a Git-free BuildKit context. A failure-first temporary fixture compares both modes, includes source-private headers, and proves Git mode excludes ignored C++ without weakening the `.git` exclusion or mounting repository metadata into the build. |
| 2026-07-24 | The first failure-first public-header check found that the repository exposed no `all_verify_interface_header_sets` target, so ordinary aggregate builds could continue masking transitive-include mistakes. CMake 4.4 already supplies the required one-translation-unit-per-header mechanism, and the existing GCC 16.1 devcontainer compiled all 15 public headers independently without source fixes. | Declare each public header once on its owning component through `FILE_SET HEADERS`, enable CMake's `VERIFY_INTERFACE_HEADER_SETS` in the four authoritative GCC workflow presets, and request the excluded aggregate verification target alongside the normal build. Explicitly disable the inherited setting for stock Clang analysis and the narrow clang-p2996 differential lane so this GCC quality gate does not expand their compiler contracts. |
| 2026-07-24 | Local GCC 16.1 Debug, Release, and ASan/UBSan devcontainer workflows passed with the public-header gate. The TSan workflow also compiled all product, test, and header-verifier translation units, but every executable aborted before `main` because Docker Desktop's Linux guest could not disable an incompatible ASLR layout. Exact published-SHA Source CI had passed the same TSan runtime on a native GitHub Linux runner. | Keep local TSan compilation as early evidence and retain the native GitHub runner as runtime authority. Do not disable ASLR on the Mac, add container privilege, weaken TSan, or classify the pre-`main` guest-runtime abort as a product regression. |
| 2026-07-24 | Corrected cache-only LLVM-analysis run `30076577966` passed the Git-free BuildKit source-enumeration boundary and reached the exact LLVM 22.1.8 formatter, which rejected recent Codex/workflow reflection and workflow fixture layout. Source CI could not catch this because formatting intentionally belongs to the separate pinned analysis toolchain, and the watcher's bounded failure tail showed only the last affected file. | Apply the LLVM 22.1.8 mechanical layout to all three affected test files, re-run their focused GCC 16.1 targets locally, and require another exact analysis run before advancing to GCC image qualification. Keep exact formatting in the medium local gate blocked on immutable analysis-devcontainer digest wiring rather than substituting an unpinned formatter as publication evidence. |
| 2026-07-24 | Exact Source CI run `30078910209` compiled every GCC 16.1 target and public-header verifier, then exposed a repeatable persistence cancellation race: NVIDIA stdexec's one-worker pool does not promise FIFO start order, so the append requested second could begin first, pass its only stop check, wait on SQLite's writer lock, and commit after cancellation. A local repeated fixture reproduced the failure. | Preserve the executor's provider contract and add no custom FIFO queue. Use Boost.SQLite's immediate RAII transaction only for asynchronous writer admission, retain deferred synchronous transactions and the pre-execution stop check, and check the propagated stop token again after writer admission but before mutation. Keep provider errors authoritative when admission fails before that checkpoint; keep the test's typed cancellation and single-row assertions while naming the cooperative boundary accurately; do not claim a hard no-commit guarantee after the final checkpoint. |
| 2026-07-24 | Plan review found that learning loops, effort escalation, bounded turns, and conservative concurrency were documented, but independent normal/adversarial review had no explicit state gate, parallel lanes had no machine-readable task/file/resource claim contract, proactive rollover required compacting the canonical thread, and the historical last-turn context claim was not wired: the Codex adapter decodes cumulative `tokenUsage.total`, while the standalone rollover compares cumulative `total_tokens`. | Add explicit review states, risk triggers, exact-fingerprint findings/dispositions, interim task capsules, and a fixture-gated atomic claim design before concurrency or autonomous merge expands. Keep the existing cumulative standalone rollover accurately documented; disable the external supervisor's percentage policy until distinct `tokenUsage.last.inputTokens` decoding passes fixtures. Roll over through a durable checkpoint into a fresh process/thread while preserving the original as read-only recovery evidence. |
| 2026-07-24 | Failure-first GCC 16.1 fixtures proved that `TokenUsage` exposed no last-turn field even though the Glaze provider DTO parsed both `tokenUsage.last` and `tokenUsage.total`; the supervisor reducer therefore received cumulative input under a misleading latest-turn contract. The first correction build also showed that fatal missing-field warnings reject positional telemetry aggregates when the domain grows. Adversarial review then proved the conversation retained a previous turn's last-input value and discarded the token notification's correlation identities; re-review caught that defaulted DTO counters accepted partial cumulative shapes and could replace preserved totals with zeros. | Preserve cumulative counters and the standalone cumulative-total rollover. Decode optional `last.inputTokens` separately, require all cumulative counters, validate provider counts at the non-negative signed 64-bit boundary, correlate token updates to the active thread/turn, clear only last-turn freshness before continuing, ignore schema-valid stale cross-turn telemetry while failing closed on malformed frames, require a fresh last-input value plus a positive window in the pure supervisor reducer, and keep scheduler totals from summing it. Use designated initialization for telemetry fixtures so cumulative and last-turn meanings remain explicit. Keep external-supervisor process activation disarmed until end-to-end telemetry/checkpoint wiring passes. |
| 2026-07-24 | Cache-only LLVM-analysis run `30080634729` preserved report-only warnings but failed on one real compiler diagnostic: Clang 22 parsed `std::map` from Ubuntu libstdc++13 and rejected C++23 heterogeneous erasure that GCC 16.1 had already accepted. The analysis toolchain existed only in the vcpkg overlay triplet, so it configured dependency builds but never reached the outer project compilation database. Repeated remediation reviews reproduced false passes for existential flags, omitted database sources, wrong or conflicting drivers/toolchains/language modes, nested and runner-supplied tidy configuration, and a runner regex that could select zero files. | Keep the product's standard-library operation and clang-tidy failure semantics unchanged. Explicitly set vcpkg's outer `VCPKG_CHAINLOAD_TOOLCHAIN_FILE` in the analysis preset; configure from a fresh CMake cache; require exact source/database completeness and every command to use pinned Clang 22 directly or through ccache, exactly C++26, and only `/opt/gcc-16.1`; prove `_GLIBCXX_RELEASE == 16`; run tidy over the entire validated database; and fixture-reject every reproduced command/configuration mutation. Run formatting before dependency installation, keep LLVM analysis AMD64-only until the separately planned native GCC ARM64 qualification passes, and do not disguise toolchain-selection failures with tidy extra arguments or product workarounds. |
| 2026-07-24 | Correctly chainloaded LLVM-analysis run `30086306340` compiled all 14 source commands against GCC 16.1 headers and eliminated the heterogeneous-erasure error, then exposed an unresolved Clang/libstdc++ complete-class constructibility interaction: Glaze could not call `optional<ResultBody::RateLimitWindowBody>::emplace()` while the scalar-defaulted aggregate was nested inside the still-forming outer DTO. Pinned Glaze, v7.9.1, and current upstream retain the same provider path; exact Clang/GCC16 probes passed when only the window DTO moved to namespace scope. The run also rebuilt GCC for 30 minutes because previous final-layer failures prevented the stable ancestor graph from reaching the GHA cache. | Preserve Glaze aggregate reflection, the scalar default, and fatal compiler diagnostics. Move only the private window DTO to `protocol_detail` namespace scope and assert aggregate, default-construction, and optional emplacement at compile time; retain runtime `{}` and `null` wire fixtures. Before validation, solve `symphony-analysis` cache-only on the same Buildx builder and export the existing `mode=min` scope so later source/tidy failures cannot discard the expensive GCC/LLVM base. Keep load and publication disabled, and require the next exact run to show both the Clang fix and cached reuse evidence before advancing. |
| 2026-07-24 | Real-time safety was requested before the service had any documented hard-deadline entry point. The scheduler, process, SQLite, and HTTP paths intentionally allocate or block, so enabling RTSan across the product would create a misleading empty signal or false contract. LLVM 22 also has no clang-tidy check that mechanically converts APIs to `span`/`string_view` or enforces `expected`. | Adopt the pinned Clang 22.1.8 Function Effect Analysis and compiler-rt RTSan first as a fixture-only preset. Make effect/noexcept diagnostics fatal only on that target, prove the runtime with safe and allocation/blocking negative controls, and prevent sanitizer flags from reaching product targets. Add only real LLVM 22 tidy checks relevant to `string_view`, optional, wakeups, coroutine locking, exceptions, and allocation/copy performance; keep them report-only until the exact Linux baseline is zero, then promote an explicit allowlist rather than a wildcard. |
| 2026-07-24 | LLVM base run `30090361901` succeeded after 1h13m38s, but the immediate exact-HEAD run `30094944459` could not reuse that result. It reached `gcc-builder`, spent nine minutes re-entering compiler construction, and failed when `ftp.gnu.org` timed out. The repository's Actions cache inventory was 11,194,039,980 bytes, above GitHub's default 10 GB allowance; the LLVM lane also authenticated and decompressed its 12 GB release tree three times. | Treat GHA cache as volatile acceleration rather than compiler identity. Persist GCC and clang-p2996 bases before their source validation and make each stable base the cache scope's only writer; gate expensive Source CI behind metadata; acquire GCC through GNU's mirror redirector with bounded all-error retry; authenticate then extract LLVM exactly once; and let clang-p2996 use up to four compile jobs while retaining one memory-heavy link job. Complete a candidate-by-digest native GHCR ceremony and move routine CI/devcontainers to reviewed immutable toolchain digests; never publish OpenSymphony. |
| 2026-07-24 | Instrumented reproduction of the pinned OpenSymphony v2.10.0 failure showed that `memory_capture_discovers_github_by_default_and_reports_missing_gh` receives an executed child status 127 with empty output when its fixture sets `PATH=""`, rather than a spawn-time `io::ErrorKind::NotFound`. The implementation therefore emits `gh exited with exit status: 127` and the unmodified upstream assertion fails. Upstream issue [#227](https://github.com/kumanday/OpenSymphony/issues/227) contains the exact public reproduction. | Keep the full upstream suite mandatory and the candidate image diagnostic-only. Do not add a repository-owned patch or classify the image as validated. Resolve upstream-first, update the immutable pin only after the corrected upstream test passes, then rebuild the local-only image and run doctor/read-only dry-run without another live canary. |
| 2026-07-24 | Exact compiler-matrix run `30097575001` persisted all three expensive AMD64 bases before validation and proved GCC 16.1 end to end in 37m20s. LLVM analysis then failed because CMake's `[[...]]` source literal ended at the first C++ attribute `]]`, so CMake 4.4 parsed the remaining probe as extra arguments. clang-p2996 reached validation after 1h33m, reached the last of 76 manifest packages, and failed while installing pinned `ut` Debug, but vcpkg and the Buildx record exposed only the path to the discarded inner log. Historical log re-review found the same masked `ut` failure in `30067119954`; missing `clang-scan-deps` after the minimized distribution is the leading explanation, but remains an inference until the inner log is retained. | Use collision-safe `[=[...]=]` literals for every RTSan C++ probe and statically reject the ambiguous delimiter. Keep the existing vcpkg manifest and overlay ownership, but on install failure emit only the six newest current-attempt nonempty build logs with bounded, secret-suppressed tails while preserving vcpkg's status. Run p2996 AMD64 alone next so the persisted base either proves the lane or exposes the exact scanner/install diagnostic. Do not rebuild a compiler on the Mac, weaken the differential contract, or guess at the `ut` failure; choose between explicitly disabling module scanning for the module-disabled header-only port and restoring `clang-scan-deps` only from that evidence. |
| 2026-07-24 | Exact p2996-only run `30106926105` proved the retained diagnostic path and failed after 1h26m29s. Its current-attempt `ut` log named `CMAKE_CXX_COMPILER_CLANG_SCAN_DEPS-NOTFOUND`; pinned `ut` disables its module file set but still creates top-level C++23 developer targets, which CMake 4.4 scans by default. BuildKit explicitly reported `clang-builder 3/3` as `CACHED`, yet materializing the prior 3.84 GB GHA record and re-exporting the unchanged base still consumed about 81 minutes, so that cache is not a viable warm validation transport. | Set `CMAKE_CXX_SCAN_FOR_MODULES=OFF` only in overlay port revision 1 while `UT_ENABLE_MODULES=OFF`; do not enlarge the minimized compiler distribution or disable scanning globally. Guard the exact port option and revision, rerun the p2996-only contract, and keep the accepted immutable-GHCR compiler-image ceremony as the durable replacement for volatile GHA compiler caches. |
| 2026-07-24 | Completed-log analysis corrected the provisional cache interpretation for run `30106926105`: BuildKit imported metadata in 0.3s and initially printed `CACHED`, then reported three referenced blobs missing. It spent 72m35s rebuilding p2996 and 2m54s exporting. The pinned Codex Universal AMD64 base is 10.17 GiB compressed by itself, so exporting a Codex-derived result into GitHub's default 10 GB repository cache necessarily evicts required content and reuploads immutable GHCR layers. | Add a `FROM scratch` p2996 artifact containing only `/opt/clang-p2996`; have the Codex-derived runtime copy from it; key its `mode=min` scope by the full compiler commit and recipe revision. Seed only through an explicit AMD64 p2996 refresh input, fail closed on persistence, and keep ordinary validation read-only. Require one seed run and a separate fresh-run no-builder sentinel before calling the interim cache healthy; immutable validated GHCR compiler digests remain the durable supply. |
| 2026-07-24 | Exact p2996-only run `30113072362` cleared the `ut` module-scanner gate, then the outer project's required reflection configure probe failed. The preset selected the p2996 compiler and vcpkg triplet but omitted the outer `VCPKG_CHAINLOAD_TOOLCHAIN_FILE`; the minimized fork therefore defaulted to the Codex base's libstdc++ instead of its installed libc++. Independent exact-commit probes compiled and linked the unchanged source with `-stdlib=libc++` and failed with libstdc++, while pinned upstream tests confirmed the selected reflection and expansion flags. | Chainload `cmake/toolchains/clang-p2996.cmake` explicitly in the outer preset, configure from a fresh CMake cache, and keep the substantive probe unchanged. Run both CI and the local p2996 devcontainer through one failure-preserving wrapper that emits a bounded secret-redacted CMake configure-log tail. Amend ADR-0001 to the already-accepted narrow `-freflection -fexpansion-statements` policy; do not restore the umbrella flag, weaken reflection, or substitute another compiler. |
| 2026-07-24 | The Actions-cache inventory reached 9.42 GB under the default 10 GB allowance, with individual BuildKit entries of 3.84 GB and 2.71 GB. Prior p2996 validation imported a cache manifest but found referenced blobs evicted and rebuilt the compiler for 72m35s. Audit confirmed that ccache already owns project compilation but is restored only after the compiler artifact boundary; sccache would consume the same constrained backend. The dotfiles workflow instead probes a recipe-addressed GHCR compiler artifact and injects its returned digest as a BuildKit named context. | Move the pinned p2996 `/opt` toolchain first to the separate `symphony-toolchain-clang-p2996` OCI package. Key its tag by compiler commit, architecture, and deterministic recipe SHA-256; globally serialize artifact workflows; require reflection, LLD, libc++, libc++abi, libunwind, and execution probes before push; carry the probe-observed or push-returned digest directly into a separate read-only source-validation job. Keep the old GHA artifact only as a one-time migration source, retain bounded ccache/vcpkg caches for ordinary source work, add no sccache, and extend the proven package-hit pattern to GCC 16.1 per native architecture. |
| 2026-07-24 | After the clang artifact boundary passed local and adversarial review, the remaining GCC matrix still wrote a Codex-derived base to the same constrained Actions cache. A failure-first contract and two review passes established the native AMD64/ARM64 package boundary, including the separate validation runner's architecture, capacity, and disk-reclaim obligations. | Add `symphony-toolchain-gcc` scratch artifacts keyed by GCC 16.1, native architecture, and synchronized recipe SHA-256. Before publication, prove exact GCC version, C++26 reflection, execution, and `/opt/gcc-16.1/lib64` resolution for both libstdc++ and libgcc. Carry the observed-or-published digest into a packages-read-only validation job, retain all Debug/Release/ASan/UBSan/TSan workflows, and remove GCC artifact `cache-to`. Treat both architecture packages as unqualified candidates until explicit native seed and fresh package-hit runs pass; do not create a multi-architecture accepted index yet. |
| 2026-07-24 | A final-image audit proved that current scratch compiler artifacts cannot retain `/tmp/gcc`, `/tmp/llvm-build`, `/tmp/llvm-project`, reflection probes, or source-validation cache mounts. Direct inspection also found that the migration-only `symphony-dev` digest still used by the GCC devcontainer and OpenSymphony base contains 335 MiB of legacy `/opt/symphony-cpp-seed/.build` and `vcpkg_installed` data even though the current Containerfile no longer creates that seed. The owner clarified that publishable C++ toolchain bases must be project-independent and reusable by other C++ repositories. | Harden and contract-test the shared `.dockerignore`, give the context-free OpenSymphony build a Dockerfile-specific deny-all ignore file, require each scratch artifact stage to have exactly one allowlisted `/opt` copy, and make generic runtime construction fail if the legacy project seed exists. Build publishable runtimes from a clean Codex Universal descendant that bind-mounts repository installation helpers without committing them to a layer; copy only the selected `/opt` toolchains into those runtimes. Use only an ephemeral BuildKit bind mount for repository validation, keep reusable-image workdirs and descriptions project-neutral, and keep the signed GCC corresponding-source archive because it is intentional distribution material. Do not call the legacy digest clean or repin any devcontainer until a replacement runtime digest is built, its filesystem is inspected, and all mutable `:edge` references are replaced through the reviewed digest-wiring ceremony. |
| 2026-07-24 | Explicit clang artifact seed run `30125124484` published immutable index `sha256:c8bc8c299bbb78713120401e68c57dd62ed56641cf7bb334993772fac3fb25d3` after 1h23m55s, then built, linked, executed, and passed the differential test from that package. Its final command audit alone failed because CMake 4.4's Ninja rule emits the valid link as `: && /opt/clang-p2996/bin/clang++ … && :`, while the verifier required the compiler at token zero. | Treat the published scratch artifact as a valid unqualified candidate and fix only the false-negative audit. Normalize the exact inert Ninja no-op prefix and suffix before applying the existing compiler/libc++/LLD/rpath checks; reject active or asymmetric envelopes with failure-first fixtures. Keep the compiler flags and toolchain unchanged, then run a fresh current-HEAD package-hit validation and prove the compiler builder does not execute before qualification advances. |
| 2026-07-24 | Fresh package-hit run `30131115313` passed in 13m11s without executing `clang-builder`, proving the p2996 compiler package is reusable. A subsequent image-boundary review found that keeping project/vcpkg validation in the generic Containerfile was structurally misleading, and that the signed OCI index digest cannot serve as the external `FROM` context even though its unique linux/amd64 child manifest can. | Keep compiler production and project validation as separate BuildKit graphs. Make generic p2996 runtime assembly fail closed unless Bake supplies an exact digest-pinned external compiler context; select and verify exactly one linux/amd64 image child from the immutable OCI index; scope ccache to that child with content-based compiler checks; run Symphony source and vcpkg only in a separate cache-only validation Dockerfile whose final scratch stage exports one pass marker. Apply the same separation to GCC and analysis only after their artifact boundaries are independently proven. |
| 2026-07-24 | Separated p2996 package-hit run `30133573373` passed in 11m32s: the resolver selected the exact linux/amd64 child, the generic runtime consumed it, and the cache-only project graph passed without publishing or loading an image. Adversarial review found the not-yet-seeded GCC lane would otherwise forward its signed OCI index rather than its native child in the same way. | Qualify the separated p2996 graph and carry the proven native-child selection ceremony into the primary GCC 16.1 lane before its first seed. Select exactly one `linux/${architecture}` OCI image manifest, retain the index only for provenance, bind the source-build cache to the selected child digest, and use content-based ccache compiler validation on both native architectures. |
| 2026-07-24 | Before the first GCC artifact seed, a containment review found that its generic Containerfile still coupled reusable compiler runtime assembly to Symphony's vcpkg install and project validation. Adversarial review then showed that an undefined `COPY --from` name falls back to a mutable registry `:latest`, and that LLVM analysis lacked the private-package login needed to consume GCC. Local failure-first contracts, omission builds, static BuildKit checks, and resolved Bake-graph inspection proved the corrected native AMD64/ARM64 design without rebuilding a compiler. | Preserve the established GCC 16.1 producer recipe and its `6b1431c…` identity, but require generic GCC and analysis runtimes to consume an exact digest-pinned external artifact context. Define empty local sentinel stages that named contexts override so omitted inputs fail deterministically without registry fallback; give analysis only package-read authentication and propagate the exact source revision. Put Symphony source, vcpkg, compiler-cache mounts, and workflow validation only in a separate cache-only Dockerfile whose final scratch target exports one marker. Resolve the signed OCI index to exactly one verified native image child, scope ccache by that child with content-based compiler checks, and prohibit loads, pushes, tags, or exported archives from validation. Track durable dependency caching in issue #2 and resolver/publisher least-privilege separation in issue #3. Seed AMD64 explicitly only after this graph passes publication checks and independent review; qualify ARM64 separately before any accepted multi-architecture runtime. |
| 2026-07-24 | Explicit AMD64 GCC seed run `30137472450` published immutable signed index `sha256:3077e36a1dc077dae525041111177967d9534784963ce53537bb8993011c5165`, selected native child `sha256:30cf91ffd71a925d11e687b31b7d298ca8b30b715b12d547ba4c1a8c4091c890`, and passed the separated project graph. Fresh seed-disabled run `30138910349` then resolved that same index and child, skipped the compiler publication step entirely, contained no `gcc-builder` execution, restored the child-scoped compiler cache, achieved 232/232 ccache hits, and passed all four 11-test workflows in 14m40s. | Qualify the separated GCC 16.1 AMD64 compiler artifact boundary. Keep its immutable child as the only compiler input to generic runtime assembly and keep Symphony source/vcpkg state confined to ephemeral validation mounts. The remaining repeated vcpkg dependency acquisition is validation acceleration work, not image content: persist only bounded ABI archives under issue #2, and never place the vcpkg checkout, installed tree, project build tree, or source in a reusable runtime. Qualify native ARM64 independently before constructing any accepted multi-architecture index or pinning the ARM64 Mac's devcontainer. |
| 2026-07-24 | GCC package-hit run `30138910349` restored 0/77 vcpkg packages on its first install and spent about 347 seconds rebuilding them, while later installs in the same BuildKit mount restored all 77 in about 0.4 seconds. The repository cache inventory had also accumulated 9,322,098,367 bytes of retired BuildKit compiler-migration blobs after both immutable compiler packages were qualified; deleting only those 30 exact migration entries reduced active cache use from 10,134,439,517 to 812,341,150 bytes without touching Source CI, ccache, vcpkg, lint, or Buildx-download entries. | Bridge only `/var/cache/vcpkg` through the already-pinned cache-dance and Actions Cache pair, using an exact key containing runner OS/architecture, resolved native triplet, selected GCC child digest, and dependency/build inputs with no restore prefix. Log archive bytes/files, retain ccache as a separate scoped mount, and continue excluding the vcpkg checkout, installed tree, port/project build trees, and source. Do not simulate a size-aware save by reserving an immutable empty key; track true restore/export/save pressure gating separately. Require one cold save and a distinct fresh exact-key hit before promoting this acceleration path. |
| 2026-07-24 | Native ARM64 seed run `30139992468` published immutable GCC artifact index `sha256:4bb2091e75a695ee17f3843cae0aa951f5077f5686683678e4b7447186bdc908` and selected child `sha256:19dea032f531be3b15ccbc04bc8795a357877a9f4ece2b0f92a0fb46858f0ee3`, then failed before project validation. A native local reproduction proved that a binary compiled in the generic runtime resolved Ubuntu's `/lib/aarch64-linux-gnu/libstdc++.so.6` and `libgcc_s.so.1`: the prior guard proved only that `/opt` appeared in the loader cache, not that it was the selected candidate. Ubuntu ARM64's `aarch64-linux-gnu.conf` sorts before the former `gcc-16.1.conf`, unlike AMD64's later-sorting `x86_64-linux-gnu.conf`. | Keep the published compiler artifact as an unqualified immutable candidate and correct only generic runtime selection: register `/opt/gcc-16.1/lib64` through lexically first `/etc/ld.so.conf.d/00-gcc-16.1.conf`, rebuild the loader cache, and avoid a process-wide `LD_LIBRARY_PATH`. Require validation with `LD_LIBRARY_PATH` unset to print and prove both libstdc++ and libgcc resolve from `/opt` before executing the C++26 probe. Preserve the artifact recipe and digest. Rerun with compiler seeding disabled, require the compiler builder to remain absent, and qualify ARM64 only after all four project workflows pass; a later distinct run must still prove the exact vcpkg archive-cache hit. |
| 2026-07-24 | Seed-disabled ARM64 run `30141979097` reused GCC child `sha256:19dea032f531be3b15ccbc04bc8795a357877a9f4ece2b0f92a0fb46858f0ee3` without `gcc-builder`, proved both GCC 16.1 runtimes under an unset `LD_LIBRARY_PATH`, and passed all four project workflows in 14m57s. Its cold first install restored 0/77 packages, populated 78 vcpkg archive files totaling 24,902,423 bytes, and saved the exact archive key. Distinct seed-disabled run `30142443867` restored that primary key, restored all 77 packages on the first install in 253 ms, achieved 232/232 ccache hits, repeated both runtime-link probes and all workflows, and passed in 6m00s. | Qualify the separated native ARM64 GCC 16.1 compiler artifact and the exact-key vcpkg archive acceleration path. Treat the 8m57s fresh-run reduction as measured validation evidence, not permission to put archives or Symphony state in a runtime image. Keep issue #2 open for pressure-aware conditional save and final cache diagrams, and issue #4 open for a separately published, inspected runnable generic runtime plus immutable devcontainer wiring. Do not assemble or promote a multi-platform runtime index from compiler-artifact qualification alone. |
| 2026-07-24 | Runtime-ceremony review found that the repository has no protected branch, ruleset, or protected publication environment, while checked-out workflow code can currently receive package-write authority. A dormant write job would still receive its token before a conditional step, and naming a nonexistent environment would create it without protection. A contained trusted-utility audit of the exact pinned Codex Universal ARM64 rootfs passed after distinguishing direct runtime credential locations from provider-owned NVM and xdebug source metadata; this proves the audit policy is compatible with the base, not that a GCC runtime candidate exists or is qualified. | Keep the first reusable-runtime slice capability-absent: add only an exact-digest BuildKit validator with a cache-only output, an exact deny-all context containing one read-only reflection fixture, network-disabled native GCC 16.1/CMake 4.4/runtime-link probes, and a merged-rootfs audit performed from hard-pinned BusyBox rather than candidate tools. Do not add a publisher job, registry output, attestation write, accepted tag, index assembly, devcontainer pin, or OpenSymphony base change until protected-main and protected-environment governance is explicitly authorized and observed. |
| 2026-07-25 | Non-publishing compiler-matrix run `30145802928` resolved the existing GCC and clang artifacts and passed both compiler lanes, then exact LLVM 22.1.8 formatting rejected the shared P2996 compiler recipe probe. Mechanically formatting that probe would rotate both byte-derived artifact recipe identities and require new package seeds even though no compiler payload changed. | Preserve the already-qualified probe bytes under one exact-path formatter exemption, contract-test both Git and Git-free discovery so no other C++ source escapes, and keep compiling/executing the probe in both compiler gates. Track a separate migration that splits compiler payload identity from probe-qualification identity; remove the exemption after that migration lets probe-only changes requalify an exact existing artifact digest without rebuilding `/opt`. |
| 2026-07-25 | Failure-first GCC 16.1 fixtures proved that the reflection seam lacked enum enumeration and that GCC implements `enumerators_of` without an access-context argument. Adversarial review then exposed an empty-enum `-Werror` edge and narrowed the storage claim. Local GCC passed all 11 tests; exact Source CI run `30146656860` passed Debug, Release, ASan/UBSan, and TSan; exact clang-p2996 differential run `30146685872` passed. | Add a `consteval` fixed `std::array<std::string_view, N>` name table in declaration order using standard C++26 `enumerators_of`, `define_static_array`, and `identifier_of`. Prove exact return type, values, order, and empty enums under both compiler authorities. Describe it as runtime-allocation-free, keep it reflection-only for now, and retain the dependency decision against adding `magic_enum`. |
| 2026-07-25 | Draft v1 §5.2 requires optional YAML front matter and a trimmed prompt body, but the loader rejected prompt-only documents and returned delimiter-framed prompts with trailing whitespace; the existing test asserted the nonconforming newline. | Treat delimiter detection as framing rather than a mandatory prefix. Parse Glaze only after an initial delimiter, otherwise retain the complete original content as the prompt, then apply the same outer trim once. Preserve interior whitespace, raw-content fingerprints, typed defaults, and fail-closed handling for unterminated, sequence, or scalar front matter. Keep loader acceptance distinct from dispatch validation and leave the §5.5 typed-error migration separate. |
| 2026-07-25 | LLVM run `30148756830` repeated the same analysis-base persistence step after an earlier manifest restored with missing blobs. The repository held 10,640,951,502 bytes of BuildKit cache blobs, beyond GitHub's default 10 GB allowance, and the workflow contradicted the existing rule against writing compiler or generic-runtime layers to Actions cache. The run was cancelled at 11m27s instead of repeating an ineffective export. | Remove the analysis-base `cache-to` entirely. Add an exact-format target with a read-only source mount and network-disabled formatter `RUN` before ccache bridging, vcpkg setup, configuration, tidy, and RTSan; give it only the existing exact GCC child and read-only layer inputs. Its signed LLVM/toolchain ancestors can still require networked materialization on a cache miss, so a separately qualified durable analysis runtime remains the path to a truly compute-early gate. Keep full validation cache-only and retain its formatting check for direct/local execution. Contract-test the step order, exact pinned action, no layer export, formatter boundary, and both targets actually invoked by CI. Track compiler-sensitive ccache identity and cache-pressure telemetry separately in issue #2. |
