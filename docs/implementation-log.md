# Implementation goal log

Append-only record for the standalone implementation.

| Date | Observation | Decision |
| --- | --- | --- |
| 2026-07-22 | A new repository avoids coupling the C++ service to the honeymoon Node controller. Both research lanes already have current artifacts. | Bootstrap a fixture-only C++ vertical slice; mark primary and Last30Days research `reused`; prohibit live mutations and publication. |
| 2026-07-22 | The initial portable Linux C++26 suite passed, then dependency-enabled Docker commands detached after the tool window and overlapping invocations corrupted Ninja's log. | Treat long compiler/build jobs as named containers and poll one owner; never start a duplicate for an unchanged running build. This is `promoted` into `scripts/container-job.sh`. |
| 2026-07-22 | Repository creation already triggered the compiler matrix; a manual dispatch queued the same revision behind it. | Cancel the duplicate immediately. Before dispatch, list runs for the exact workflow and commit; dispatch only when no queued or running equivalent exists. |
| 2026-07-22 | GCC 16.1 surfaced a deprecation warning inside nlohmann/json under the project's `-Werror` policy. | Mark fetched third-party dependencies as CMake `SYSTEM`; retain strict warnings for project source. |
| 2026-07-22 | The owner wants to choose the C++ infrastructure libraries before the scaffold expands. | Freeze the library decision boundary; continue only architecture-neutral protocol, conformance, source-lock, and CI work until those choices are agreed. |
| 2026-07-22 | The workspace-level commit hook evaluates this standalone repository against an expired controller lease in the separate honeymoon worktree. | Do not bypass the hook or mutate the other worktree. Preserve the staged changes and treat repository-local commit authorization as unresolved infrastructure. |
| 2026-07-22 | The owner selected Glaze, `openalgz/ut`, and vcpkg, and requested modern hosted async/persistence candidates plus broad static analysis. | Adopt one Glaze codec seam, keep Codex on JSONL/JSON-RPC, migrate dependencies through a pinned vcpkg manifest, migrate tests to `ut`, and evaluate SQLite plus a serialized executor before a bounded `sqlgen` spike. Reject the Intel bare-metal stack for the hosted daemon. |
| 2026-07-22 | The recent-practitioner scan was noisy and its web/YouTube lanes failed, while primary project documentation directly answered the architecture questions. | Record `no-new-lesson`: retain the raw report as discovery evidence, supplement it with owning sources, and do not let social ranking override primary documentation. |
| 2026-07-22 | GCC 16.1 rejected an allocation-backed reflection query used directly as an expansion-statement range. | Bind `nonstatic_data_members_of` to a `constexpr` local before each expansion, matching current P2996 usage; require the remote GCC 16 reflection job as the regression guard. |
| 2026-07-22 | The first reflection correction still retained the allocation-backed range and failed on GCC 16.1. GCC's implementation/tests require static promotion for expansion. | Correct the prior decision: wrap each reflected member range with `std::define_static_array`; preserve the failed CI run as regression evidence and rerun the exact remote job. |
| 2026-07-22 | The owner selected `klemens-morgenstern/sqlite`; source inspection found strong typed/RAII SQLite coverage but no async API or curated vcpkg port. | Adopt it only inside `symphony_persistence`, package it through a pinned overlay, and execute it on the serialized DB executor behind repository-owned sender/error/schema boundaries. |
| 2026-07-22 | Custom bootstrap mechanisms accumulated before all third-party choices were made. The owner requires third-party libraries, tools, generators, or permitted services whenever they already provide a capability. | Promote a mandatory dependency-first skill, governing convention, decision ledger, AGENTS rule, CI review surface, and automated ban on FetchContent/CPM/ExternalProject bypasses. Custom replacements now require explicit capability-specific owner authorization. |
| 2026-07-22 | `std::define_static_array` fixed allocation lifetime but GCC 16.1 still rejected a function-local non-static range as an expansion source and explicitly diagnosed the missing storage duration. | Make the promoted range `static constexpr`; if the exact GCC job still fails at this invariant, stop local reflection repair and replace the bootstrap meta implementation with Glaze's already-tested P2996 backend under the new dependency-first policy. |
| 2026-07-22 | The new dependency-policy job ran before vcpkg/build work but the minimal GCC container did not contain the guard's Node runtime. | Install the distribution-provided `nodejs` build tool in the existing frontend step; keep policy enforcement before dependency resolution and compilation so violations fail cheaply. |
| 2026-07-22 | The guard's use of `git ls-files` inherited Git's container ownership check even though the policy only needs CMake inputs. | Remove the Git dependency; walk CMake inputs with the Node standard library while excluding build, generated, vcpkg, and Git metadata trees. |
| 2026-07-22 | The local test registry duplicated a capability already provided by the owner-selected `openalgz/ut`. | Delete the custom registry/macros, package `ut` v1.2.0 through a pinned SHA-512-verified vcpkg overlay, and require its exported `ut::ut` target. The overlay passes vcpkg's enforced port checks on macOS; GCC 16.1 CI remains the authoritative suite. |
| 2026-07-22 | The initial checkout lives under a Codex-managed worktree path that is not an appropriate durable project home. | After a clean verified push, establish the canonical local checkout at `~/dev/symphony-cpp`; verify branch, HEAD, remote, and clean status there before retiring the temporary worktree. |
| 2026-07-22 | The verified `ut` checkpoint was cleanly cloned at `~/dev/symphony-cpp` on the exact pushed commit while the Codex-managed worktree remained intact. | Use the durable checkout as the canonical working copy; retire the temporary worktree only after no active task depends on it. |
| 2026-07-22 | Glaze 7.9.0 passed focused typed DTO, tolerant decode, generic object, JSON-schema, and vcpkg gates; the remaining nlohmann/json paths duplicated those capabilities. | Replace nlohmann/json with Glaze in reflected serialization/schema and Codex JSONL payloads, preserve only bounded newline framing and protocol sequencing, and guard additive fields plus invalid response identifiers with fixtures. |
