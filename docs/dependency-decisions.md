# Dependency decision ledger

Record a row before implementing or materially expanding a capability. `Custom` is permitted only
with a link or quote identifying explicit owner authorization.

| Capability | Selected provider | Why this owns the mechanism | Status / re-evaluation trigger |
| --- | --- | --- | --- |
| JSON, reflected codecs, schema helpers | Glaze | One C++23/26 codec/reflection stack covers JSON and related formats. | Active; nlohmann/json removed after fixture parity. GCC 16.1 owns full codec/schema semantics. The pinned clang-p2996 fork is differential-only at the dependency-free P2996 adapter because it asserts inside Glaze's unrelated fallback field-name machinery. Re-evaluate the narrow boundary when the fork compiles the pinned Glaze release. |
| Versioned controller task, result, review, and notepad records | Glaze 7.9.0 typed JSON and JSON Schema generation, pinned `check-jsonschema` 0.37.4 fixture validation, CMake 4.4 exact-result orchestration, and C++23/26 `std::expected` at the repository codec seam | The selected codec already provides compile-time aggregate reflection, deterministic direct-to-object JSON, strict unknown/missing-key options, schema `required` generation, `additionalProperties: false`, top-level version/kind metadata constants, and bounded `std::span` output with an explicit overflow result. The pinned external validator independently exercises generated structural schemas against single-mutation repository fixtures; the CMake adapter distinguishes validator rejection from operational failure and applies the same one-MiB file boundary. Standard `std::expected` keeps Glaze errors and exceptions out of the public control-record boundary. | Adopt for `TaskPacketV1`, `TaskResultV1`, `ReviewAttestationV1`, and `TaskNotepadV1`. Keep Glaze types private to the adapter and store owned strings at the durable-record boundary. Generate schemas into the build tree rather than hand-maintaining schema JSON. Cross-field review-pass consistency is a symmetric runtime invariant because Glaze 7.9.0 does not expose JSON Schema `if`/`then` metadata. This slice excludes identity/digest recomputation, replay/expiry/authentication, controller authority, atomic claims, persistence, tracker mutation, redaction-owned sinks, and specialist admission. Re-evaluate if the pinned Glaze release cannot express a required structural/version constraint, adds maintained duplicate-key rejection or cross-field schema metadata, or if generated schemas fail the pinned independent validator. |
| Operator HTTP API | Glaze HTTP | Existing ASIO HTTP/REST implementation avoids a local server/router. | Prototype gate: cancellation, TLS, size limits, overload, shutdown. |
| Codex JSON-RPC payloads | Glaze JSON facilities plus newline framing adapter | Existing codec facilities own JSON parsing and serialization; local code owns only Codex-specific DTOs, state sequencing, and JSONL process framing. | Active; additive-field and malformed-ID fixtures guard compatibility. |
| Workflow YAML | Glaze 7.9.0 YAML | The selected codec provides typed YAML 1.2 block/flow parsing, block scalars, generic provider subtrees, unknown-key policy, recursion bounds, and serialization without a second parser. | Adopted; `yaml-cpp` removed after synthetic shape, provider round-trip, repository `WORKFLOW.md`, GCC 16.1, ASan/UBSan, and TSan gates passed. Keep environment expansion, path policy, front-matter framing, and prompt semantics outside the codec. |
| Unit tests | `openalgz/ut` | Existing C++23 runtime/compile-time test framework replaces the local registry/macros. | Active through the pinned vcpkg overlay; GCC 16.1 CI is the release gate. The overlay disables both upstream modules and CMake's automatic scan only while packaging this header-only dependency, because upstream's top-level developer targets otherwise require a separately omitted `clang-scan-deps`; re-enable and qualify the scanner before enabling modules. |
| Dependency management | vcpkg manifest mode | Pinned registry, version graph, binary caching, and overlay mechanism. | Active; FetchContent/CPM/vendoring prohibited. |
| Toolchain base images and development containers | Pinned Docker Buildx actions for current qualification and planned validated immutable GHCR compiler images for stable CI supply; Dev Container Specification and reference CLI for local image creation, lifecycle, and execution | Docker owns OCI construction and exact-digest promotion. GHCR is the selected durable compiler-artifact store once the guarded ceremony exists; BuildKit caches remain rebuild accelerators only. Current cache-only validation descendants exercise exact source ancestry without importing another multi-gigabyte copy into Docker Engine. A separate exact-digest, cache-only BuildKit graph validates the merged filesystem of a runnable generic runtime without loading it into the Docker Engine or giving the validator package-write authority. The Dev Container CLI owns the developer-only derived image, workspace/cache mounts, lifecycle commands, user environment, and `exec` semantics. The system dynamic loader owns default GCC runtime resolution: the image registers `/opt/gcc-16.1/lib64` in lexically first `/etc/ld.so.conf.d/00-gcc-16.1.conf` and rebuilds the cache rather than exporting a process-wide `LD_LIBRARY_PATH`. | Current: immutable scratch compiler packages are qualified independently from runnable generic runtimes. The eventual generic runtime must be built from the pinned Codex Universal base plus an exact qualified compiler child and must never contain Symphony source, vcpkg archives or installed trees, repository/build state, OpenSymphony, or credentials. The checked-in validator graph is locally callable with an exact digest and has no registry output; no GitHub runtime-candidate workflow or publisher exists yet. Runtime publication, signed attestation, package naming, and later multiarch assembly remain disabled until protected-main and protected-environment authority is approved. Validate native children separately and record the reviewed index digest before replacing any local `:edge` reference. Never publish OpenSymphony or a fully configured repository devcontainer. See [`docs/toolchain-and-devcontainer-workflow.md`](toolchain-and-devcontainer-workflow.md). |
| BuildKit validation-cache persistence | Pinned `reproducible-containers/buildkit-cache-dance`, `actions/cache`, and BusyBox transport only the size-bounded ccache mount and vcpkg's exactly scoped ABI-addressed binary-archive mount; compiler identity remains a separate `FROM scratch` GHCR artifact | Cache dance bridges explicit BuildKit cache mounts without committing them to an image. ccache owns compiler-result validity and vcpkg owns the compressed package archive format and ABI keys; the vcpkg checkout, installed tree, build trees, project build tree, and source remain disposable. GHA cache is mutable acceleration, never validated compiler identity. | Run `30106926105` proved that full-image cache metadata could hit while evicted blobs forced a 72m35s rebuild; the 10.17 GiB Codex AMD64 image already exceeds GitHub's default repository allowance. Retire the one-time compiler-migration scopes after immutable GHCR qualification and never write compiler or generic-runtime layers back to Actions cache. Scope both validation caches by lineage, native architecture, selected compiler child, and dependency/build contract; restore vcpkg archives only by an exact key and keep every checkout/installed/build/source path out of Actions cache and reusable images. Log vcpkg archive bytes and files on every validation. Retain the current child-scoped ccache generation until measured rotation is implemented. A cache miss must rebuild correctly; a true size/pressure-aware conditional save requires split restore/export/save orchestration and must not be simulated by reserving an immutable empty key. |
| Durable compiler artifact transport | Separate GHCR scratch OCI images built and pushed by pinned Docker Buildx actions, probed by a compiler/architecture/recipe-hash tag, captured by manifest digest, and injected into validation as a named build context | GHCR is the maintained package store already selected for compiler images and is directly consumable by Docker and local devcontainers. A complete `/opt` toolchain is the reusable artifact; ccache and BuildKit registry caches are rebuild accelerators, not its identity. GitHub workflow artifacts are run-scoped and expire, while the repository's Actions cache is already 9.42 GB and cannot currently exceed the default 10 GB allowance. | Use separate `symphony-toolchain-clang-p2996` and `symphony-toolchain-gcc` packages, with GCC 16.1 keyed independently per native architecture. Hash the pinned base, build recipe, runtime set, installer, and prepublication probe into each tag; serialize only duplicate publication for the same artifact key; carry the digest observed by the probe or returned by the push directly into source validation without re-resolving the tag. Publish only after compiler-integrity, C++26 reflection, linker, and `/opt` runtime-resolution probes pass. A later source-validation failure must retain the candidate. Never overwrite an existing recipe tag or promote a validated tag before the complete lane passes. |
| Local fail-fast hooks and workflow lint | pre-commit 4.6.1 with exact top-level project pins for actionlint 1.7.12, zizmor 1.28.0, check-jsonschema 0.37.4, and ShellCheck 0.11.0; mise records artifact checksums when the backend exposes them. `jdx/mise-action` 4.2.1 commit `dad1bfd3df957f44999b559dd69dc1671cb4e9ea` installs locked tools under mise 2026.7.12 with Linux X64 binary SHA-256 `dad54e0b843908324282b8673f9c0ebc3a4da0c49ad2da309a49bfbc918ba180`. | Maintained analyzers own shell, GitHub Actions expression/schema/security, and hook orchestration. mise owns unprivileged exact-version installation and provenance lock data. Git's maintained `ls-files` and `grep --cached`/`--untracked --no-exclude-standard` surfaces own index/worktree truth; a thin Bash adapter owns only the repository's required reset paths, archive exclusions, and error policy. No second scanner framework is introduced. | Run staged diff, shell syntax, ShellCheck warnings, GitHub workflow expression/schema/security, JSON, dependency, LLVM-version, platform, canonical-goal routing, and push-route fixtures before commits and in Source CI. Hostile fixtures reject stale goal references, Git-inspection errors, invalid push refs, wrong routes, and non-checkpoint drift. Local devcontainer setup installs locked tools and both hook types into persistent caches. The pre-push hook never runs Buildx for documentation/control/self changes; later source, devcontainer, and container routes remain fail-closed until their exact phase validations are admitted. The pipx check-jsonschema backend pins the top-level version but not every Python transitive artifact; evaluate a hash-locked Python environment before claiming full transitive reproducibility. Keep shfmt, hadolint, and default yamllint nonblocking until their existing debt is fixed in a contained baseline. Exact LLVM 22.1.8 formatting/tidy remains inside the analysis validation environment. |
| Change-routed exact-HEAD push receipt | Git 2.50.1 exact commits, ancestry, symbolic refs, and rename-aware status; pre-commit 4.6.1 pre-push metadata; pinned Node.js descriptor APIs (`openSync` with `O_NOFOLLOW`, `fstatSync`, bounded `readSync`, and `closeSync`); existing redacted publication scan | Git owns range/ref truth, pre-commit owns hook dispatch, Node provides NUL-safe routing and one-open same-descriptor receipt verification, and the existing scan owns the receipt bytes. A general publisher protocol was rejected because it duplicated maintained mechanisms, exceeded 1,000 lines, conflicted with checkpoint writes, and circularly blocked R2. Official sources: [Git pre-push](https://git-scm.com/docs/githooks#_pre_push) and [pre-commit pre-push](https://pre-commit.com/#pre-push). | Support one normal fast-forward push of the current checked-out existing branch. Before transport, `prepare` runs documentation/control or hook-self quick preflight plus the exact-range scan without Docker within 60 seconds. The hook verifies the strict zero-finding exact-base/head receipt twice, including immediately after its final context recheck, and requires it to be at most 15 minutes old within 5 seconds. Later routes fail closed until admitted; only goal/notepad checkpoint drift is allowed. The pre-commit adapter exposes one proposed-update namespace, so this does not claim multi-ref enforcement. Client hooks remain local mistake-prevention; exact-HEAD Source CI is authoritative. |
| Reproducible formatting and static analysis | Official architecture-matched LLVM 22.1.8 Linux X64/ARM64 binary releases (`llvmorg-22.1.8`) in a separate analysis devcontainer | One upstream-maintained tool stack supplies exact-version `clang-format`, `clang-tidy`, the Clang static analyzer, and `run-clang-tidy.py` without reimplementing checks or compiling LLVM in this repository. The official release assets are content-addressed and carry upstream signatures and GitHub artifact attestations. | Adopt for reflection-disabled analysis only. GCC 16.1 remains authoritative for executable semantics and Bloomberg clang-p2996 remains differential-only. The outer analysis preset must explicitly set vcpkg's chainload toolchain so every analyzed project source command selects pinned Clang 22, exactly C++26, and `/opt/gcc-16.1` headers and runtime rather than the container's Ubuntu libstdc++; validation must prove source/database completeness, inspect every compilation-database entry, reject compiler target, sysroot, and C++ standard-library include redirection, and verify `_GLIBCXX_RELEASE == 16`. Configure analysis from a fresh CMake cache while retaining provider caches. Make formatting fatal before dependency installation, with a read-only source mount and a network-disabled formatter `RUN`, and consume existing analysis layers with `cache-from` only: formatting must never export the multi-gigabyte analysis ancestry to Actions cache. Ancestor materialization may still use the network on a cache miss; eliminating that prerequisite requires a separately qualified durable analysis runtime. Retain the exact byte-stable `tests/fixtures/p2996_reflection_probe.cpp` exemption while its bytes remain part of both published immutable compiler recipe identities; contract-test that narrow exemption and remove it after payload and qualification identities are separated. Keep the complete formatting check in full analysis as a direct/local safety invariant. Start tidy over the complete validated database as a narrow measured report with behavioral guards against YAML or runner config/warning promotion, promote only a zero-noise project baseline, and never auto-fix in CI. Keep architecture-specific analysis caches and images separate from both compiler lineages. Re-evaluate at each LLVM release or if an official binary cannot use GCC 16.1's libstdc++ and the pinned vcpkg graph. |
| Public-header self-containment | CMake 4.4 `FILE_SET HEADERS` plus `VERIFY_INTERFACE_HEADER_SETS` | CMake generates one translation unit per public or interface header and compiles it with the owning target's usage requirements. This replaces handwritten include-only fixtures or a repository-owned source generator while keeping GCC 16.1 diagnostics authoritative. | Adopt for every installed/public header set. Build CMake's aggregate verification target in all GCC workflow presets, including Release and sanitizer configurations, but keep the dependency-free clang-p2996 differential target narrow. Model headers on their actual component targets so an overly broad aggregate link interface cannot mask a missing include or dependency. |
| Fast Source CI caches | `actions/cache` 5.0.5 at `27d5ce7f107fe9357f9df03efb73ab90386fccae` plus vcpkg's filesystem binary cache and ccache | The pinned Node 24 action owns GitHub cache transport; vcpkg owns ABI-keyed dependency archives; ccache owns compiler-result validity. This removes repeated provider builds without caching configured build trees or inventing a package service. | Cache vcpkg archives under the pinned dependency graph and restore a bounded GCC 16.1 ccache lineage per commit. Keep misses non-fatal, retain configure/build/test on every run, and rotate the key version when compiler or cache policy changes. |
| Overlay dependency pin maintenance | vcpkg immutable `REF`/`SHA512` inputs, GitHub Actions schedules, and GitHub's maintained `gh` CLI | vcpkg owns source-integrity verification; GitHub owns scheduling, repository-scoped authentication, fast-forward branch publication, and draft-PR creation. Repository code owns only the allowlisted mapping between upstream branches, overlay files, hashes, and lock documentation. The reviewed `peter-evans/create-pull-request` alternative was rejected because its update path uses force-with-lease, which violates repository publication policy. | Run weekly or manually with least-privilege `contents: write` and `pull-requests: write`. Skip when an earlier updater PR remains open; otherwise use one run-unique branch, one ordinary push, and one reviewable draft PR. Never auto-merge, rebase, or force-push. Do not persist checkout credentials; mount repository Git metadata read-only during third-party builds, pass no token into the build container, fingerprint the generated patch across validation, and configure `gh` authentication only immediately before publication. Validate the generated patch in the updater job because `GITHUB_TOKEN` activity does not automatically provide a trusted green gate. Stop for a separate migration when a dependency appears in the pinned curated vcpkg registry. |
| Dynamic memory and undefined-behavior analysis | GCC 16.1 AddressSanitizer plus UndefinedBehaviorSanitizer | The authoritative compiler already supplies maintained instrumentation and runtimes for heap/stack memory safety and undefined behavior, so a separate analyzer framework would duplicate the mechanism. | Run ASan+UBSan together in a dedicated GCC devcontainer preset with frame pointers and strict failure options. Keep TSan separate because sanitizer runtimes are not composed into one binary. |
| Real-time constraint analysis | Clang 22.1.8 Function Effect Analysis plus compiler-rt RealtimeSanitizer from the pinned LLVM analysis toolchain | Clang owns the compile-time `nonblocking`/`nonallocating` contract and the maintained runtime interceptors for allocation, deallocation, locks, and explicitly blocking calls. This avoids a repository-owned real-time checker and adds no product dependency. | Adopt first as a Clang-only provider fixture with fatal `-Wfunction-effects` and `-Wperf-constraint-implies-noexcept`, frame pointers, interceptor verification, one safe case, and allocation/blocking negative controls. Keep it separate from GCC semantic authority, reflection, ASan/UBSan, and TSan. Do not annotate a product path until its real-time deadline, ownership, preallocation, nonblocking call graph, and benchmark contract are documented; the current scheduler, process, database, and HTTP paths intentionally allocate or block. |
| Formatting | `{fmt}` | Maintained type-safe formatting; compatible with future standard formatting. | Selected; use only after redaction. |
| Compile-time composition and structure | Intel compile-time-init-build plus cpp-std-extensions at immutable latest-main overlay pins; C++26 reflection, Glaze, and `openalgz/ut` at their existing seams | CIB's tuple-free `lookup::make` replaces handwritten exact HTTP-status branching with a consteval table. `stdx` and Boost.MP11 own its compile-time value/type machinery; standard reflection owns structural field and enumerator discovery plus identifier extraction; Glaze owns codec metadata; `ut` owns compile-time tests. | Owner-directed adoption. Source CI runs `30040027088` and `30040593648` proved that the pinned CIB callback/nexus path ICEs GCC 16.1 in `stdx::tuple`; do not instantiate that path until a future pin passes a standalone GCC probe. Exact provider/product fixtures gate every atomic pin update. Keep enum-name tables runtime-allocation-free and reflection-only rather than adding `magic_enum`; wire them into product enums only after both GCC 16.1 authority and clang-p2996 differential fixtures pass. Intel bare-metal concurrency and senders/receivers remain isolated compatibility fixtures; NVIDIA stdexec remains the hosted executor. See [`docs/research/compile-time-library-review.md`](research/compile-time-library-review.md). |
| Structured logging sinks | spdlog 1.17.0 from the pinned vcpkg registry | Its synchronous multi-sink logger, per-sink exception interception, per-logger error handler, and existing `{fmt}` integration provide the sink mechanism without adding a background runtime. | Adopt behind `EventStore`. Redact and retain the bounded status event before dispatch, count sink failures without recursive logging, and emit JSON as the unadorned payload. Re-evaluate only if required durability or asynchronous backpressure cannot be provided by configured spdlog sinks. |
| Async execution | NVIDIA stdexec `fee4d651494014610a277540f209cae56011e47f` behind a project execution seam | GCC 16.1 does not document native P2300 senders/receivers; the pinned registry package supplies the maintained C++26 reference model, static thread pool, value/error/stopped channels, and structured composition without a repository-owned executor. | Adopted from the curated port after exact GCC 16.1 provider and scheduler overlap gates. Keyed stop sources cancel individual invocations; only the scheduler drains completions and mutates run state. Executor capacity is fixed at daemon startup, so a live increase requires restart. Replace with the standard library once GCC provides the required surface. |
| SQLite access | `klemens-morgenstern/sqlite` | Typed queries, prepared statements, non-throwing APIs, RAII transactions/savepoints, hooks and backup. | Active through the pinned overlay. Synchronous calls stay on a one-thread NVIDIA stdexec pool and retain deferred transactions; restart, invalid-envelope, concurrent-append, GCC 16.1, ASan/UBSan, and TSan fixtures passed. NVIDIA stdexec owns queued execution and stop-token propagation without promising FIFO start order. Asynchronous appends use Boost.SQLite's immediate RAII transaction as the writer-admission boundary and check cooperative cancellation before mutation. Stop observed after admission rolls back through the transaction guard; provider errors, including writer-admission timeout, retain precedence if admission fails before that checkpoint. After the final checkpoint, cancellation remains cooperative rather than a hard no-commit guarantee. The repository applies a startup-fixed admission limit and returns a typed overload error before submission so accepted asynchronous work remains bounded. |
| Durable schema / repository | SQLite migrations plus repository-owned SQL | Durable domain schema is product-specific; wrapper owns mechanism, repository owns Symphony mapping. | Initial explicit versioned event envelope and ordered append/load repository are active. SQLite `user_version` records the committed schema level; startup applies ordered forward migrations transactionally and rejects databases newer than the executable. Pinned sqlgen 0.6.0 failed the deletion gate because its documented write path auto-creates schema from reflected structs, exposes `rfl::Result`, adds reflect-cpp/CTRE/yyjson, and supplies no cancellation boundary. Re-evaluate only if it adds an explicit-migration/manual-schema mode that deletes measured mapper code without introducing a second reflection or result authority. |
| Durable structured-event fanout | Thin adapter over `EventStore` and `EventRepository` | Redaction, JSON rendering, bounded async admission, serialized execution, and SQLite durability are already owned by selected components; the adapter owns only the Symphony mapping and failure accounting. | Fixture gate: redact before both sinks, retain the operator event when persistence overloads, count immediate and completion failures, and drain accepted work before destruction. Do not add another queue, serializer, logger, or database API. |
| Child process lifecycle | Boost.Process v2 1.91.0 | Its Asio-native process, stdio, EOF, working-directory, child-environment initializer, wait, and cancellation facilities replace repository-owned fork/exec/pipe/environment/wait mechanics. The pinned GCC 16.1 gate covers separate stdout/stderr, explicit EOF, start-directory, filtered environment, termination, and reap fixtures. | Adopted. Snapshot the child environment before concurrent workers start, remove every secret name published by registered tracker profiles, and pass the remaining entries through `process_environment`; preserve Codex-required non-secret variables. Use a dedicated Asio stderr pipe for a bounded tail while stdout remains authoritative. Keep Boost types behind the adapter. Process-tree/group cancellation remains an owner-gated hardening extension. |
| Workspace key and path containment | PicoSHA2 1.0.1 plus C++26 string/filesystem facilities behind `WorkspaceExecutor` | The curated, MIT, header-only dependency owns SHA-256; the standard library owns portable path resolution and inspection; Draft v1 owns allowed-character replacement and the stable original-identifier hash rule. | Active. Preserve `[A-Za-z0-9._-]`, append 128 SHA-256 bits when replacement is required, make `.`/`..` safe, and revalidate immediately before every hook or removal. Fixture and local executors must pass the same containment contract. |
| CLI parsing | CLI11 2.6.2 from the pinned vcpkg registry | Glaze 7.9.0's CLI facility is an interactive reflected menu rather than an argv parser. CLI11 directly owns strict positional/option parsing, subcommands, standard help, parse diagnostics, and exit codes without runtime dependencies. | Adopt behind executable-specific option structs. Keep workflow loading and process lifecycle outside the parser; re-evaluate if Glaze adds an equivalent argv contract. |
| Exact decimal arithmetic | Boost.Decimal | Existing IEEE decimal types. | Add only when an upstream contract requires exact decimals. |
| Symphony domain transitions and scheduler policy | OpenAI Symphony Draft v1 implemented locally | Normative product behavior has no reusable conforming C++ implementation in the recorded search; the owner explicitly approved the standalone C++ plan. | Keep pure and fixture-driven; commodity mechanisms still require dependencies. |
| Objective-progress fingerprint and typed evidence projection | PicoSHA2 1.0.1 from the pinned vcpkg registry plus C++26 owned values, comparisons, ranges, and fixed-width typed framing behind `symphony::domain::fingerprint` | The project already depends on the curated MIT SHA-256 provider and its incremental API, so the domain does not need a repository-owned hash algorithm or another dependency. Standard facilities own typed evidence values, normalization, duplicate removal, and framing primitives. Reusing control-record DTOs would invert the domain/control layering and hash non-objective log/environment metadata; Glaze serialization would add intermediate JSON allocation and codec coupling without verifying evidence. | Migrate the published v1 interim identity frame through an explicit v2 contract using repository path/content-digest and command/cwd/toolchain/exit/artifact-digest evidence. Narrative, tokens, timestamps, run/session IDs, repeated identical evidence, process success alone, and unsupported completion claims remain excluded. Typed values are claims until the separately gated verifier recomputes allowed-path/artifact digests and accepts command results from the trusted executor; production scheduling remains fail-closed until that verifier and `TaskResultV1` projection are wired. Plan and tracker/PR transitions remain separate later evidence classes. |
| Tracker adapter contract and normalization | C++26 `std::expected` plus Glaze DTO facilities behind the provider-neutral tracker seam | The Draft v1 issue model, adapter profiles, pagination safety, and error taxonomy are Symphony integration policy; standard vocabulary types carry failures without a custom result framework, while the selected codec will own future provider payload decoding. | Fixture-only contract active. Live HTTP remains disabled until the recorded Glaze HTTP client gate passes; do not add a second JSON or transport stack. |
| Development-work orchestrator | OpenSymphony `v2.10.0` as a separate local-only container | It supplies Linear-driven scheduling, isolated issue workspaces, retries, recovery, Codex and OpenHands harnessing, gateway, memory, task-graph, and operator surfaces while leaving the standalone C++ product implementation and interactive devcontainer independent. | Owner-approved full-feature evaluation before any feature is disabled. Build the pinned upstream workspace locally in a multi-stage image, run its full tests unprivileged, and compose the final image through BuildKit target contexts from the clean generic GCC runtime plus its independent rootfs/runtime validation markers. The OpenSymphony Dockerfile must fail closed without those named contexts and must never fall back to the contaminated legacy `symphony-dev` image. This local composition does not publish or qualify a reusable runtime package. Do not install OpenSymphony in the devcontainer, publish its image, link or vendor Rust into the C++ service, or share the Docker socket. Prove destructive paths only with disposable repositories and fake Linear/GitHub/model endpoints; keep `GUI-5` as the sole live canary. `run --dry-run` is a no-model execution that can still write contained state and run hooks, not a read-only operation. |
| OpenSymphony fake Linear acceptance boundary | Python 3 standard-library `ThreadingHTTPServer`, already present in the pinned local GCC base | A maintained, already-installed HTTP mechanism is enough for the upstream-compatible fixture and avoids another image, package manager, or custom production transport. | Fixture-only. Return the minimum active/query shapes, reject every GraphQL mutation fail-closed, and audit only operation names without headers or bodies. It is not production code, a tracker client, or a reusable service. |
| OpenSymphony AST acceptance | OpenSymphony v2.10.0's pinned Tree-sitter/query-pack and lightweight parser stack, exercised through its existing read-only memory MCP endpoint | The purpose is to verify the upstream parser lanes and provenance, not to add a parser or code-intelligence implementation to this repository. Existing shell, temporary fixture files, `curl`, and Python standard-library JSON assertions are sufficient test drivers. | Fixture-only and read-only. Cover Rust, TypeScript, TSX, JavaScript, JSX, Python, JSON, YAML, TOML, and Markdown; require C++ to remain explicitly unsupported; fingerprint memory state before and after. Do not infer persistent code-graph or C++ support from this gate. |
| Workflow numeric representability | C++26 `std::in_range` from the standard library | Workflow YAML is decoded into a lossless `std::uint64_t` staging type before selected limits enter the domain's `std::uint32_t` fields. The standard checked-integer predicate exactly owns this representability boundary without a custom arithmetic layer. | Reject `max_concurrent_agents`, `max_turns`, and per-state concurrency values above `UINT32_MAX` before conversion. Keep Intel Safe Arithmetic deferred while its upstream project labels itself non-production; reevaluate only if the domain requires checked arithmetic beyond standard conversion predicates. |
| Coding-worker model policy | Codex CLI model catalog, app-server pass-through fields, repository-owned non-secret config overlays, and an external C++26 supervisor | Codex's versioned schema owns model/effort vocabulary. OpenSymphony can select one model but exposes no effective effort route or bounded lifetime policy in v2.10.0, while the standalone app-server accepts `model` and `effort` per turn. | Use the verified Sol/high baseline. Between completed worker sessions, select only preapproved Sol/xhigh or Sol/max overlays for cross-subsystem design or typed product/test/compiler no-progress and repeated-failure results. Credential, authority, missing-telemetry, external-service, and resource failures pause or route to deterministic handling rather than spending more effort. De-escalate only after a changed progress fingerprint and fresh green review; never duplicate the Codex catalog or replace OpenSymphony scheduling. Pause after two consecutive eligible no-progress/repeated-failure results, four accepted model-backed sessions, 90 minutes, missing required telemetry, or a failed checkpoint. |
| Coding-worker context rollover | Codex app-server token/context telemetry plus repository-owned session policy | Codex owns token measurement and compaction; Symphony owns the product decision to stop sending continuation turns before a configured percentage of the reported context window. No commodity scheduler can make this repository-specific transition. | The existing daemon app-server continuation path conservatively applies cumulative `tokenUsage.total.totalTokens` only between completed turns with a positive model context window. The adapter also decodes optional `tokenUsage.last.inputTokens`, requires the complete cumulative shape, validates provider counts as non-negative signed 64-bit values, correlates telemetry to its thread/turn, ignores schema-valid stale updates, fails closed on malformed frames, and clears last-turn freshness before a continuation while retaining cumulative evidence. Separately, the disarmed external-supervisor reducer uses only the fresh last-turn value, checkpoints at 45%, starts handoff without new scope at 50%, refuses another turn at 55%, and pauses when the last-turn value or positive window is missing. Keep that external process disarmed until its complete telemetry/checkpoint wiring passes; the existing app-server path still stops after observed compaction, and neither path may estimate missing telemetry. |
| Native specialist-agent orchestration | Codex project custom agents, the official OpenAI Developer Docs MCP, repository skills, and the existing `gh-watch-run` skill | Current Codex owns subagent spawning, model/effort selection, sandbox inheritance, concurrency limits, plugin/skill discovery, and thread lifecycle. Project profiles add only bounded role instructions and provisional schema-shaped report contracts; the primary controller retains task ordering and goal writes. Existing skills own dependency-first evaluation, upstream watching, and one-run CI monitoring. | Configured as an event-triggered advisory layer in `.codex/agents/`; profiles remain unadmitted until read-only parent/tool isolation and hostile input fixtures pass. No custom scheduler, standing model poller, tracker writer, or plugin is introduced. `.codex/config.toml` caps spawned threads at three as an emergency ceiling, while normal dispatch uses at most one specialist until representative fixtures measure defect recall, latency, token cost, cancellation, and false positives. Routine delta collection and unchanged routing use no model. Re-evaluate when those fixtures pass or the pinned Codex custom-agent schema changes. |

Comparative evidence and access dates live in [`docs/research/sources.md`](research/sources.md) and
[`docs/research/cpp-library-evaluation.md`](research/cpp-library-evaluation.md).
